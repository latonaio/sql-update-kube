// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformAllergenAllergenTextDatum is an object representing the database table.
type DataPlatformAllergenAllergenTextDatum struct {
	Allergen     string      `boil:"Allergen" json:"Allergen" toml:"Allergen" yaml:"Allergen"`
	Language     string      `boil:"Language" json:"Language" toml:"Language" yaml:"Language"`
	AllergenName null.String `boil:"AllergenName" json:"AllergenName,omitempty" toml:"AllergenName" yaml:"AllergenName,omitempty"`

	R *dataPlatformAllergenAllergenTextDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformAllergenAllergenTextDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformAllergenAllergenTextDatumColumns = struct {
	Allergen     string
	Language     string
	AllergenName string
}{
	Allergen:     "Allergen",
	Language:     "Language",
	AllergenName: "AllergenName",
}

var DataPlatformAllergenAllergenTextDatumTableColumns = struct {
	Allergen     string
	Language     string
	AllergenName string
}{
	Allergen:     "data_platform_allergen_allergen_text_data.Allergen",
	Language:     "data_platform_allergen_allergen_text_data.Language",
	AllergenName: "data_platform_allergen_allergen_text_data.AllergenName",
}

// Generated where

var DataPlatformAllergenAllergenTextDatumWhere = struct {
	Allergen     whereHelperstring
	Language     whereHelperstring
	AllergenName whereHelpernull_String
}{
	Allergen:     whereHelperstring{field: "`data_platform_allergen_allergen_text_data`.`Allergen`"},
	Language:     whereHelperstring{field: "`data_platform_allergen_allergen_text_data`.`Language`"},
	AllergenName: whereHelpernull_String{field: "`data_platform_allergen_allergen_text_data`.`AllergenName`"},
}

// DataPlatformAllergenAllergenTextDatumRels is where relationship names are stored.
var DataPlatformAllergenAllergenTextDatumRels = struct {
	LanguageDataPlatformLanguageLanguageDatum string
	AllergenDataPlatformAllergenAllergenDatum string
}{
	LanguageDataPlatformLanguageLanguageDatum: "LanguageDataPlatformLanguageLanguageDatum",
	AllergenDataPlatformAllergenAllergenDatum: "AllergenDataPlatformAllergenAllergenDatum",
}

// dataPlatformAllergenAllergenTextDatumR is where relationships are stored.
type dataPlatformAllergenAllergenTextDatumR struct {
	LanguageDataPlatformLanguageLanguageDatum *DataPlatformLanguageLanguageDatum `boil:"LanguageDataPlatformLanguageLanguageDatum" json:"LanguageDataPlatformLanguageLanguageDatum" toml:"LanguageDataPlatformLanguageLanguageDatum" yaml:"LanguageDataPlatformLanguageLanguageDatum"`
	AllergenDataPlatformAllergenAllergenDatum *DataPlatformAllergenAllergenDatum `boil:"AllergenDataPlatformAllergenAllergenDatum" json:"AllergenDataPlatformAllergenAllergenDatum" toml:"AllergenDataPlatformAllergenAllergenDatum" yaml:"AllergenDataPlatformAllergenAllergenDatum"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformAllergenAllergenTextDatumR) NewStruct() *dataPlatformAllergenAllergenTextDatumR {
	return &dataPlatformAllergenAllergenTextDatumR{}
}

func (r *dataPlatformAllergenAllergenTextDatumR) GetLanguageDataPlatformLanguageLanguageDatum() *DataPlatformLanguageLanguageDatum {
	if r == nil {
		return nil
	}
	return r.LanguageDataPlatformLanguageLanguageDatum
}

func (r *dataPlatformAllergenAllergenTextDatumR) GetAllergenDataPlatformAllergenAllergenDatum() *DataPlatformAllergenAllergenDatum {
	if r == nil {
		return nil
	}
	return r.AllergenDataPlatformAllergenAllergenDatum
}

// dataPlatformAllergenAllergenTextDatumL is where Load methods for each relationship are stored.
type dataPlatformAllergenAllergenTextDatumL struct{}

var (
	dataPlatformAllergenAllergenTextDatumAllColumns            = []string{"Allergen", "Language", "AllergenName"}
	dataPlatformAllergenAllergenTextDatumColumnsWithoutDefault = []string{"Allergen", "Language", "AllergenName"}
	dataPlatformAllergenAllergenTextDatumColumnsWithDefault    = []string{}
	dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns     = []string{"Allergen", "Language"}
	dataPlatformAllergenAllergenTextDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformAllergenAllergenTextDatumSlice is an alias for a slice of pointers to DataPlatformAllergenAllergenTextDatum.
	// This should almost always be used instead of []DataPlatformAllergenAllergenTextDatum.
	DataPlatformAllergenAllergenTextDatumSlice []*DataPlatformAllergenAllergenTextDatum

	dataPlatformAllergenAllergenTextDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformAllergenAllergenTextDatumType                 = reflect.TypeOf(&DataPlatformAllergenAllergenTextDatum{})
	dataPlatformAllergenAllergenTextDatumMapping              = queries.MakeStructMapping(dataPlatformAllergenAllergenTextDatumType)
	dataPlatformAllergenAllergenTextDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns)
	dataPlatformAllergenAllergenTextDatumInsertCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenTextDatumInsertCache          = make(map[string]insertCache)
	dataPlatformAllergenAllergenTextDatumUpdateCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenTextDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformAllergenAllergenTextDatumUpsertCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenTextDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformAllergenAllergenTextDatum record from the query.
func (q dataPlatformAllergenAllergenTextDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformAllergenAllergenTextDatum, error) {
	o := &DataPlatformAllergenAllergenTextDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_allergen_allergen_text_data")
	}

	return o, nil
}

// All returns all DataPlatformAllergenAllergenTextDatum records from the query.
func (q dataPlatformAllergenAllergenTextDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformAllergenAllergenTextDatumSlice, error) {
	var o []*DataPlatformAllergenAllergenTextDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformAllergenAllergenTextDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformAllergenAllergenTextDatum records in the query.
func (q dataPlatformAllergenAllergenTextDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_allergen_allergen_text_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformAllergenAllergenTextDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_allergen_allergen_text_data exists")
	}

	return count > 0, nil
}

// LanguageDataPlatformLanguageLanguageDatum pointed to by the foreign key.
func (o *DataPlatformAllergenAllergenTextDatum) LanguageDataPlatformLanguageLanguageDatum(mods ...qm.QueryMod) dataPlatformLanguageLanguageDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`Language` = ?", o.Language),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformLanguageLanguageData(queryMods...)
}

// AllergenDataPlatformAllergenAllergenDatum pointed to by the foreign key.
func (o *DataPlatformAllergenAllergenTextDatum) AllergenDataPlatformAllergenAllergenDatum(mods ...qm.QueryMod) dataPlatformAllergenAllergenDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`Allergen` = ?", o.Allergen),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformAllergenAllergenData(queryMods...)
}

// LoadLanguageDataPlatformLanguageLanguageDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformAllergenAllergenTextDatumL) LoadLanguageDataPlatformLanguageLanguageDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformAllergenAllergenTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformAllergenAllergenTextDatum
	var object *DataPlatformAllergenAllergenTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformAllergenAllergenTextDatum.(*DataPlatformAllergenAllergenTextDatum)
		if !ok {
			object = new(DataPlatformAllergenAllergenTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformAllergenAllergenTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformAllergenAllergenTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformAllergenAllergenTextDatum.(*[]*DataPlatformAllergenAllergenTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformAllergenAllergenTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformAllergenAllergenTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformAllergenAllergenTextDatumR{}
		}
		args = append(args, object.Language)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformAllergenAllergenTextDatumR{}
			}

			for _, a := range args {
				if a == obj.Language {
					continue Outer
				}
			}

			args = append(args, obj.Language)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_language_language_data`),
		qm.WhereIn(`data_platform_language_language_data.Language in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformLanguageLanguageDatum")
	}

	var resultSlice []*DataPlatformLanguageLanguageDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformLanguageLanguageDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_language_language_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_language_language_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.LanguageDataPlatformLanguageLanguageDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Language == foreign.Language {
				local.R.LanguageDataPlatformLanguageLanguageDatum = foreign
				break
			}
		}
	}

	return nil
}

// LoadAllergenDataPlatformAllergenAllergenDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformAllergenAllergenTextDatumL) LoadAllergenDataPlatformAllergenAllergenDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformAllergenAllergenTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformAllergenAllergenTextDatum
	var object *DataPlatformAllergenAllergenTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformAllergenAllergenTextDatum.(*DataPlatformAllergenAllergenTextDatum)
		if !ok {
			object = new(DataPlatformAllergenAllergenTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformAllergenAllergenTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformAllergenAllergenTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformAllergenAllergenTextDatum.(*[]*DataPlatformAllergenAllergenTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformAllergenAllergenTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformAllergenAllergenTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformAllergenAllergenTextDatumR{}
		}
		args = append(args, object.Allergen)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformAllergenAllergenTextDatumR{}
			}

			for _, a := range args {
				if a == obj.Allergen {
					continue Outer
				}
			}

			args = append(args, obj.Allergen)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_allergen_allergen_data`),
		qm.WhereIn(`data_platform_allergen_allergen_data.Allergen in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformAllergenAllergenDatum")
	}

	var resultSlice []*DataPlatformAllergenAllergenDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformAllergenAllergenDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_allergen_allergen_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_allergen_allergen_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.AllergenDataPlatformAllergenAllergenDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Allergen == foreign.Allergen {
				local.R.AllergenDataPlatformAllergenAllergenDatum = foreign
				break
			}
		}
	}

	return nil
}

// SetLanguageDataPlatformLanguageLanguageDatum of the dataPlatformAllergenAllergenTextDatum to the related item.
// Sets o.R.LanguageDataPlatformLanguageLanguageDatum to related.
func (o *DataPlatformAllergenAllergenTextDatum) SetLanguageDataPlatformLanguageLanguageDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformLanguageLanguageDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_allergen_allergen_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"Language"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.Language, o.Allergen, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Language = related.Language
	if o.R == nil {
		o.R = &dataPlatformAllergenAllergenTextDatumR{
			LanguageDataPlatformLanguageLanguageDatum: related,
		}
	} else {
		o.R.LanguageDataPlatformLanguageLanguageDatum = related
	}

	return nil
}

// SetAllergenDataPlatformAllergenAllergenDatum of the dataPlatformAllergenAllergenTextDatum to the related item.
// Sets o.R.AllergenDataPlatformAllergenAllergenDatum to related.
func (o *DataPlatformAllergenAllergenTextDatum) SetAllergenDataPlatformAllergenAllergenDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformAllergenAllergenDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_allergen_allergen_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"Allergen"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.Allergen, o.Allergen, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Allergen = related.Allergen
	if o.R == nil {
		o.R = &dataPlatformAllergenAllergenTextDatumR{
			AllergenDataPlatformAllergenAllergenDatum: related,
		}
	} else {
		o.R.AllergenDataPlatformAllergenAllergenDatum = related
	}

	return nil
}

// DataPlatformAllergenAllergenTextData retrieves all the records using an executor.
func DataPlatformAllergenAllergenTextData(mods ...qm.QueryMod) dataPlatformAllergenAllergenTextDatumQuery {
	mods = append(mods, qm.From("`data_platform_allergen_allergen_text_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_allergen_allergen_text_data`.*"})
	}

	return dataPlatformAllergenAllergenTextDatumQuery{q}
}

// FindDataPlatformAllergenAllergenTextDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformAllergenAllergenTextDatum(ctx context.Context, exec boil.ContextExecutor, allergen string, language string, selectCols ...string) (*DataPlatformAllergenAllergenTextDatum, error) {
	dataPlatformAllergenAllergenTextDatumObj := &DataPlatformAllergenAllergenTextDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_allergen_allergen_text_data` where `Allergen`=? AND `Language`=?", sel,
	)

	q := queries.Raw(query, allergen, language)

	err := q.Bind(ctx, exec, dataPlatformAllergenAllergenTextDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_allergen_allergen_text_data")
	}

	return dataPlatformAllergenAllergenTextDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformAllergenAllergenTextDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_allergen_allergen_text_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformAllergenAllergenTextDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformAllergenAllergenTextDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenTextDatumInsertCache[key]
	dataPlatformAllergenAllergenTextDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformAllergenAllergenTextDatumAllColumns,
			dataPlatformAllergenAllergenTextDatumColumnsWithDefault,
			dataPlatformAllergenAllergenTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_allergen_allergen_text_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_allergen_allergen_text_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_allergen_allergen_text_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_allergen_allergen_text_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.Allergen,
		o.Language,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_allergen_allergen_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformAllergenAllergenTextDatumInsertCacheMut.Lock()
		dataPlatformAllergenAllergenTextDatumInsertCache[key] = cache
		dataPlatformAllergenAllergenTextDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformAllergenAllergenTextDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformAllergenAllergenTextDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformAllergenAllergenTextDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenTextDatumUpdateCache[key]
	dataPlatformAllergenAllergenTextDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformAllergenAllergenTextDatumAllColumns,
			dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_allergen_allergen_text_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_allergen_allergen_text_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, append(wl, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_allergen_allergen_text_data row")
	}

	if !cached {
		dataPlatformAllergenAllergenTextDatumUpdateCacheMut.Lock()
		dataPlatformAllergenAllergenTextDatumUpdateCache[key] = cache
		dataPlatformAllergenAllergenTextDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformAllergenAllergenTextDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_allergen_allergen_text_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformAllergenAllergenTextDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_allergen_allergen_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformAllergenAllergenTextDatum slice")
	}

	return nil
}

var mySQLDataPlatformAllergenAllergenTextDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformAllergenAllergenTextDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_allergen_allergen_text_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformAllergenAllergenTextDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformAllergenAllergenTextDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformAllergenAllergenTextDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenTextDatumUpsertCache[key]
	dataPlatformAllergenAllergenTextDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformAllergenAllergenTextDatumAllColumns,
			dataPlatformAllergenAllergenTextDatumColumnsWithDefault,
			dataPlatformAllergenAllergenTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformAllergenAllergenTextDatumAllColumns,
			dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_allergen_allergen_text_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_allergen_allergen_text_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_allergen_allergen_text_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_allergen_allergen_text_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformAllergenAllergenTextDatumType, dataPlatformAllergenAllergenTextDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_allergen_allergen_text_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_allergen_allergen_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformAllergenAllergenTextDatumUpsertCacheMut.Lock()
		dataPlatformAllergenAllergenTextDatumUpsertCache[key] = cache
		dataPlatformAllergenAllergenTextDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformAllergenAllergenTextDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformAllergenAllergenTextDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformAllergenAllergenTextDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformAllergenAllergenTextDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_allergen_allergen_text_data` WHERE `Allergen`=? AND `Language`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_allergen_allergen_text_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformAllergenAllergenTextDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformAllergenAllergenTextDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_allergen_allergen_text_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformAllergenAllergenTextDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_allergen_allergen_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformAllergenAllergenTextDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformAllergenAllergenTextDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformAllergenAllergenTextDatum(ctx, exec, o.Allergen, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformAllergenAllergenTextDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformAllergenAllergenTextDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_allergen_allergen_text_data`.* FROM `data_platform_allergen_allergen_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformAllergenAllergenTextDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformAllergenAllergenTextDatumExists checks if the DataPlatformAllergenAllergenTextDatum row exists.
func DataPlatformAllergenAllergenTextDatumExists(ctx context.Context, exec boil.ContextExecutor, allergen string, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_allergen_allergen_text_data` where `Allergen`=? AND `Language`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, allergen, language)
	}
	row := exec.QueryRowContext(ctx, sql, allergen, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_allergen_allergen_text_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformAllergenAllergenTextDatum row exists.
func (o *DataPlatformAllergenAllergenTextDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformAllergenAllergenTextDatumExists(ctx, exec, o.Allergen, o.Language)
}
