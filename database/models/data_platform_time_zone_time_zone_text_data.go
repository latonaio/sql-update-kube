// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformTimeZoneTimeZoneTextDatum is an object representing the database table.
type DataPlatformTimeZoneTimeZoneTextDatum struct {
	TimeZone     string      `boil:"TimeZone" json:"TimeZone" toml:"TimeZone" yaml:"TimeZone"`
	Language     string      `boil:"Language" json:"Language" toml:"Language" yaml:"Language"`
	TimeZoneName null.String `boil:"TimeZoneName" json:"TimeZoneName,omitempty" toml:"TimeZoneName" yaml:"TimeZoneName,omitempty"`

	R *dataPlatformTimeZoneTimeZoneTextDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformTimeZoneTimeZoneTextDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformTimeZoneTimeZoneTextDatumColumns = struct {
	TimeZone     string
	Language     string
	TimeZoneName string
}{
	TimeZone:     "TimeZone",
	Language:     "Language",
	TimeZoneName: "TimeZoneName",
}

var DataPlatformTimeZoneTimeZoneTextDatumTableColumns = struct {
	TimeZone     string
	Language     string
	TimeZoneName string
}{
	TimeZone:     "data_platform_time_zone_time_zone_text_data.TimeZone",
	Language:     "data_platform_time_zone_time_zone_text_data.Language",
	TimeZoneName: "data_platform_time_zone_time_zone_text_data.TimeZoneName",
}

// Generated where

var DataPlatformTimeZoneTimeZoneTextDatumWhere = struct {
	TimeZone     whereHelperstring
	Language     whereHelperstring
	TimeZoneName whereHelpernull_String
}{
	TimeZone:     whereHelperstring{field: "`data_platform_time_zone_time_zone_text_data`.`TimeZone`"},
	Language:     whereHelperstring{field: "`data_platform_time_zone_time_zone_text_data`.`Language`"},
	TimeZoneName: whereHelpernull_String{field: "`data_platform_time_zone_time_zone_text_data`.`TimeZoneName`"},
}

// DataPlatformTimeZoneTimeZoneTextDatumRels is where relationship names are stored.
var DataPlatformTimeZoneTimeZoneTextDatumRels = struct {
	TimeZoneDataPlatformTimeZoneTimeZoneDatum string
}{
	TimeZoneDataPlatformTimeZoneTimeZoneDatum: "TimeZoneDataPlatformTimeZoneTimeZoneDatum",
}

// dataPlatformTimeZoneTimeZoneTextDatumR is where relationships are stored.
type dataPlatformTimeZoneTimeZoneTextDatumR struct {
	TimeZoneDataPlatformTimeZoneTimeZoneDatum *DataPlatformTimeZoneTimeZoneDatum `boil:"TimeZoneDataPlatformTimeZoneTimeZoneDatum" json:"TimeZoneDataPlatformTimeZoneTimeZoneDatum" toml:"TimeZoneDataPlatformTimeZoneTimeZoneDatum" yaml:"TimeZoneDataPlatformTimeZoneTimeZoneDatum"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformTimeZoneTimeZoneTextDatumR) NewStruct() *dataPlatformTimeZoneTimeZoneTextDatumR {
	return &dataPlatformTimeZoneTimeZoneTextDatumR{}
}

func (r *dataPlatformTimeZoneTimeZoneTextDatumR) GetTimeZoneDataPlatformTimeZoneTimeZoneDatum() *DataPlatformTimeZoneTimeZoneDatum {
	if r == nil {
		return nil
	}
	return r.TimeZoneDataPlatformTimeZoneTimeZoneDatum
}

// dataPlatformTimeZoneTimeZoneTextDatumL is where Load methods for each relationship are stored.
type dataPlatformTimeZoneTimeZoneTextDatumL struct{}

var (
	dataPlatformTimeZoneTimeZoneTextDatumAllColumns            = []string{"TimeZone", "Language", "TimeZoneName"}
	dataPlatformTimeZoneTimeZoneTextDatumColumnsWithoutDefault = []string{"TimeZone", "Language", "TimeZoneName"}
	dataPlatformTimeZoneTimeZoneTextDatumColumnsWithDefault    = []string{}
	dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns     = []string{"TimeZone", "Language"}
	dataPlatformTimeZoneTimeZoneTextDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformTimeZoneTimeZoneTextDatumSlice is an alias for a slice of pointers to DataPlatformTimeZoneTimeZoneTextDatum.
	// This should almost always be used instead of []DataPlatformTimeZoneTimeZoneTextDatum.
	DataPlatformTimeZoneTimeZoneTextDatumSlice []*DataPlatformTimeZoneTimeZoneTextDatum

	dataPlatformTimeZoneTimeZoneTextDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformTimeZoneTimeZoneTextDatumType                 = reflect.TypeOf(&DataPlatformTimeZoneTimeZoneTextDatum{})
	dataPlatformTimeZoneTimeZoneTextDatumMapping              = queries.MakeStructMapping(dataPlatformTimeZoneTimeZoneTextDatumType)
	dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns)
	dataPlatformTimeZoneTimeZoneTextDatumInsertCacheMut       sync.RWMutex
	dataPlatformTimeZoneTimeZoneTextDatumInsertCache          = make(map[string]insertCache)
	dataPlatformTimeZoneTimeZoneTextDatumUpdateCacheMut       sync.RWMutex
	dataPlatformTimeZoneTimeZoneTextDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformTimeZoneTimeZoneTextDatumUpsertCacheMut       sync.RWMutex
	dataPlatformTimeZoneTimeZoneTextDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformTimeZoneTimeZoneTextDatum record from the query.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformTimeZoneTimeZoneTextDatum, error) {
	o := &DataPlatformTimeZoneTimeZoneTextDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_time_zone_time_zone_text_data")
	}

	return o, nil
}

// All returns all DataPlatformTimeZoneTimeZoneTextDatum records from the query.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformTimeZoneTimeZoneTextDatumSlice, error) {
	var o []*DataPlatformTimeZoneTimeZoneTextDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformTimeZoneTimeZoneTextDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformTimeZoneTimeZoneTextDatum records in the query.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_time_zone_time_zone_text_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_time_zone_time_zone_text_data exists")
	}

	return count > 0, nil
}

// TimeZoneDataPlatformTimeZoneTimeZoneDatum pointed to by the foreign key.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) TimeZoneDataPlatformTimeZoneTimeZoneDatum(mods ...qm.QueryMod) dataPlatformTimeZoneTimeZoneDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`TimeZone` = ?", o.TimeZone),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformTimeZoneTimeZoneData(queryMods...)
}

// LoadTimeZoneDataPlatformTimeZoneTimeZoneDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformTimeZoneTimeZoneTextDatumL) LoadTimeZoneDataPlatformTimeZoneTimeZoneDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformTimeZoneTimeZoneTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformTimeZoneTimeZoneTextDatum
	var object *DataPlatformTimeZoneTimeZoneTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformTimeZoneTimeZoneTextDatum.(*DataPlatformTimeZoneTimeZoneTextDatum)
		if !ok {
			object = new(DataPlatformTimeZoneTimeZoneTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformTimeZoneTimeZoneTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformTimeZoneTimeZoneTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformTimeZoneTimeZoneTextDatum.(*[]*DataPlatformTimeZoneTimeZoneTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformTimeZoneTimeZoneTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformTimeZoneTimeZoneTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformTimeZoneTimeZoneTextDatumR{}
		}
		args = append(args, object.TimeZone)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformTimeZoneTimeZoneTextDatumR{}
			}

			for _, a := range args {
				if a == obj.TimeZone {
					continue Outer
				}
			}

			args = append(args, obj.TimeZone)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_time_zone_time_zone_data`),
		qm.WhereIn(`data_platform_time_zone_time_zone_data.TimeZone in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformTimeZoneTimeZoneDatum")
	}

	var resultSlice []*DataPlatformTimeZoneTimeZoneDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformTimeZoneTimeZoneDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_time_zone_time_zone_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_time_zone_time_zone_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TimeZoneDataPlatformTimeZoneTimeZoneDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TimeZone == foreign.TimeZone {
				local.R.TimeZoneDataPlatformTimeZoneTimeZoneDatum = foreign
				break
			}
		}
	}

	return nil
}

// SetTimeZoneDataPlatformTimeZoneTimeZoneDatum of the dataPlatformTimeZoneTimeZoneTextDatum to the related item.
// Sets o.R.TimeZoneDataPlatformTimeZoneTimeZoneDatum to related.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) SetTimeZoneDataPlatformTimeZoneTimeZoneDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformTimeZoneTimeZoneDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_time_zone_time_zone_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"TimeZone"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.TimeZone, o.TimeZone, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TimeZone = related.TimeZone
	if o.R == nil {
		o.R = &dataPlatformTimeZoneTimeZoneTextDatumR{
			TimeZoneDataPlatformTimeZoneTimeZoneDatum: related,
		}
	} else {
		o.R.TimeZoneDataPlatformTimeZoneTimeZoneDatum = related
	}

	return nil
}

// DataPlatformTimeZoneTimeZoneTextData retrieves all the records using an executor.
func DataPlatformTimeZoneTimeZoneTextData(mods ...qm.QueryMod) dataPlatformTimeZoneTimeZoneTextDatumQuery {
	mods = append(mods, qm.From("`data_platform_time_zone_time_zone_text_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_time_zone_time_zone_text_data`.*"})
	}

	return dataPlatformTimeZoneTimeZoneTextDatumQuery{q}
}

// FindDataPlatformTimeZoneTimeZoneTextDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformTimeZoneTimeZoneTextDatum(ctx context.Context, exec boil.ContextExecutor, timeZone string, language string, selectCols ...string) (*DataPlatformTimeZoneTimeZoneTextDatum, error) {
	dataPlatformTimeZoneTimeZoneTextDatumObj := &DataPlatformTimeZoneTimeZoneTextDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_time_zone_time_zone_text_data` where `TimeZone`=? AND `Language`=?", sel,
	)

	q := queries.Raw(query, timeZone, language)

	err := q.Bind(ctx, exec, dataPlatformTimeZoneTimeZoneTextDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_time_zone_time_zone_text_data")
	}

	return dataPlatformTimeZoneTimeZoneTextDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_time_zone_time_zone_text_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformTimeZoneTimeZoneTextDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformTimeZoneTimeZoneTextDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformTimeZoneTimeZoneTextDatumInsertCache[key]
	dataPlatformTimeZoneTimeZoneTextDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformTimeZoneTimeZoneTextDatumAllColumns,
			dataPlatformTimeZoneTimeZoneTextDatumColumnsWithDefault,
			dataPlatformTimeZoneTimeZoneTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_time_zone_time_zone_text_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_time_zone_time_zone_text_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_time_zone_time_zone_text_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_time_zone_time_zone_text_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.TimeZone,
		o.Language,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_time_zone_time_zone_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformTimeZoneTimeZoneTextDatumInsertCacheMut.Lock()
		dataPlatformTimeZoneTimeZoneTextDatumInsertCache[key] = cache
		dataPlatformTimeZoneTimeZoneTextDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformTimeZoneTimeZoneTextDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformTimeZoneTimeZoneTextDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformTimeZoneTimeZoneTextDatumUpdateCache[key]
	dataPlatformTimeZoneTimeZoneTextDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformTimeZoneTimeZoneTextDatumAllColumns,
			dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_time_zone_time_zone_text_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_time_zone_time_zone_text_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, append(wl, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_time_zone_time_zone_text_data row")
	}

	if !cached {
		dataPlatformTimeZoneTimeZoneTextDatumUpdateCacheMut.Lock()
		dataPlatformTimeZoneTimeZoneTextDatumUpdateCache[key] = cache
		dataPlatformTimeZoneTimeZoneTextDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_time_zone_time_zone_text_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformTimeZoneTimeZoneTextDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_time_zone_time_zone_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformTimeZoneTimeZoneTextDatum slice")
	}

	return nil
}

var mySQLDataPlatformTimeZoneTimeZoneTextDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_time_zone_time_zone_text_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformTimeZoneTimeZoneTextDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformTimeZoneTimeZoneTextDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformTimeZoneTimeZoneTextDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformTimeZoneTimeZoneTextDatumUpsertCache[key]
	dataPlatformTimeZoneTimeZoneTextDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformTimeZoneTimeZoneTextDatumAllColumns,
			dataPlatformTimeZoneTimeZoneTextDatumColumnsWithDefault,
			dataPlatformTimeZoneTimeZoneTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformTimeZoneTimeZoneTextDatumAllColumns,
			dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_time_zone_time_zone_text_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_time_zone_time_zone_text_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_time_zone_time_zone_text_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_time_zone_time_zone_text_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformTimeZoneTimeZoneTextDatumType, dataPlatformTimeZoneTimeZoneTextDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_time_zone_time_zone_text_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_time_zone_time_zone_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformTimeZoneTimeZoneTextDatumUpsertCacheMut.Lock()
		dataPlatformTimeZoneTimeZoneTextDatumUpsertCache[key] = cache
		dataPlatformTimeZoneTimeZoneTextDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformTimeZoneTimeZoneTextDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformTimeZoneTimeZoneTextDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_time_zone_time_zone_text_data` WHERE `TimeZone`=? AND `Language`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_time_zone_time_zone_text_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformTimeZoneTimeZoneTextDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformTimeZoneTimeZoneTextDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_time_zone_time_zone_text_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformTimeZoneTimeZoneTextDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_time_zone_time_zone_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformTimeZoneTimeZoneTextDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformTimeZoneTimeZoneTextDatum(ctx, exec, o.TimeZone, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformTimeZoneTimeZoneTextDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformTimeZoneTimeZoneTextDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_time_zone_time_zone_text_data`.* FROM `data_platform_time_zone_time_zone_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTimeZoneTimeZoneTextDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformTimeZoneTimeZoneTextDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformTimeZoneTimeZoneTextDatumExists checks if the DataPlatformTimeZoneTimeZoneTextDatum row exists.
func DataPlatformTimeZoneTimeZoneTextDatumExists(ctx context.Context, exec boil.ContextExecutor, timeZone string, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_time_zone_time_zone_text_data` where `TimeZone`=? AND `Language`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, timeZone, language)
	}
	row := exec.QueryRowContext(ctx, sql, timeZone, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_time_zone_time_zone_text_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformTimeZoneTimeZoneTextDatum row exists.
func (o *DataPlatformTimeZoneTimeZoneTextDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformTimeZoneTimeZoneTextDatumExists(ctx, exec, o.TimeZone, o.Language)
}
