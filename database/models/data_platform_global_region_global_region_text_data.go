// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformGlobalRegionGlobalRegionTextDatum is an object representing the database table.
type DataPlatformGlobalRegionGlobalRegionTextDatum struct {
	GlobalRegion     string      `boil:"GlobalRegion" json:"GlobalRegion" toml:"GlobalRegion" yaml:"GlobalRegion"`
	Language         string      `boil:"Language" json:"Language" toml:"Language" yaml:"Language"`
	GlobalRegionName null.String `boil:"GlobalRegionName" json:"GlobalRegionName,omitempty" toml:"GlobalRegionName" yaml:"GlobalRegionName,omitempty"`

	R *dataPlatformGlobalRegionGlobalRegionTextDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformGlobalRegionGlobalRegionTextDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformGlobalRegionGlobalRegionTextDatumColumns = struct {
	GlobalRegion     string
	Language         string
	GlobalRegionName string
}{
	GlobalRegion:     "GlobalRegion",
	Language:         "Language",
	GlobalRegionName: "GlobalRegionName",
}

var DataPlatformGlobalRegionGlobalRegionTextDatumTableColumns = struct {
	GlobalRegion     string
	Language         string
	GlobalRegionName string
}{
	GlobalRegion:     "data_platform_global_region_global_region_text_data.GlobalRegion",
	Language:         "data_platform_global_region_global_region_text_data.Language",
	GlobalRegionName: "data_platform_global_region_global_region_text_data.GlobalRegionName",
}

// Generated where

var DataPlatformGlobalRegionGlobalRegionTextDatumWhere = struct {
	GlobalRegion     whereHelperstring
	Language         whereHelperstring
	GlobalRegionName whereHelpernull_String
}{
	GlobalRegion:     whereHelperstring{field: "`data_platform_global_region_global_region_text_data`.`GlobalRegion`"},
	Language:         whereHelperstring{field: "`data_platform_global_region_global_region_text_data`.`Language`"},
	GlobalRegionName: whereHelpernull_String{field: "`data_platform_global_region_global_region_text_data`.`GlobalRegionName`"},
}

// DataPlatformGlobalRegionGlobalRegionTextDatumRels is where relationship names are stored.
var DataPlatformGlobalRegionGlobalRegionTextDatumRels = struct {
	GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum string
}{
	GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum: "GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum",
}

// dataPlatformGlobalRegionGlobalRegionTextDatumR is where relationships are stored.
type dataPlatformGlobalRegionGlobalRegionTextDatumR struct {
	GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum *DataPlatformGlobalRegionGlobalRegionDatum `boil:"GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum" json:"GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum" toml:"GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum" yaml:"GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformGlobalRegionGlobalRegionTextDatumR) NewStruct() *dataPlatformGlobalRegionGlobalRegionTextDatumR {
	return &dataPlatformGlobalRegionGlobalRegionTextDatumR{}
}

func (r *dataPlatformGlobalRegionGlobalRegionTextDatumR) GetGlobalRegionDataPlatformGlobalRegionGlobalRegionDatum() *DataPlatformGlobalRegionGlobalRegionDatum {
	if r == nil {
		return nil
	}
	return r.GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum
}

// dataPlatformGlobalRegionGlobalRegionTextDatumL is where Load methods for each relationship are stored.
type dataPlatformGlobalRegionGlobalRegionTextDatumL struct{}

var (
	dataPlatformGlobalRegionGlobalRegionTextDatumAllColumns            = []string{"GlobalRegion", "Language", "GlobalRegionName"}
	dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithoutDefault = []string{"GlobalRegion", "Language", "GlobalRegionName"}
	dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithDefault    = []string{}
	dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns     = []string{"GlobalRegion", "Language"}
	dataPlatformGlobalRegionGlobalRegionTextDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformGlobalRegionGlobalRegionTextDatumSlice is an alias for a slice of pointers to DataPlatformGlobalRegionGlobalRegionTextDatum.
	// This should almost always be used instead of []DataPlatformGlobalRegionGlobalRegionTextDatum.
	DataPlatformGlobalRegionGlobalRegionTextDatumSlice []*DataPlatformGlobalRegionGlobalRegionTextDatum

	dataPlatformGlobalRegionGlobalRegionTextDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformGlobalRegionGlobalRegionTextDatumType                 = reflect.TypeOf(&DataPlatformGlobalRegionGlobalRegionTextDatum{})
	dataPlatformGlobalRegionGlobalRegionTextDatumMapping              = queries.MakeStructMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType)
	dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns)
	dataPlatformGlobalRegionGlobalRegionTextDatumInsertCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionTextDatumInsertCache          = make(map[string]insertCache)
	dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformGlobalRegionGlobalRegionTextDatum record from the query.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformGlobalRegionGlobalRegionTextDatum, error) {
	o := &DataPlatformGlobalRegionGlobalRegionTextDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_global_region_global_region_text_data")
	}

	return o, nil
}

// All returns all DataPlatformGlobalRegionGlobalRegionTextDatum records from the query.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformGlobalRegionGlobalRegionTextDatumSlice, error) {
	var o []*DataPlatformGlobalRegionGlobalRegionTextDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformGlobalRegionGlobalRegionTextDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformGlobalRegionGlobalRegionTextDatum records in the query.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_global_region_global_region_text_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_global_region_global_region_text_data exists")
	}

	return count > 0, nil
}

// GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum pointed to by the foreign key.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum(mods ...qm.QueryMod) dataPlatformGlobalRegionGlobalRegionDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`GlobalRegion` = ?", o.GlobalRegion),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformGlobalRegionGlobalRegionData(queryMods...)
}

// LoadGlobalRegionDataPlatformGlobalRegionGlobalRegionDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformGlobalRegionGlobalRegionTextDatumL) LoadGlobalRegionDataPlatformGlobalRegionGlobalRegionDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformGlobalRegionGlobalRegionTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformGlobalRegionGlobalRegionTextDatum
	var object *DataPlatformGlobalRegionGlobalRegionTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformGlobalRegionGlobalRegionTextDatum.(*DataPlatformGlobalRegionGlobalRegionTextDatum)
		if !ok {
			object = new(DataPlatformGlobalRegionGlobalRegionTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformGlobalRegionGlobalRegionTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformGlobalRegionGlobalRegionTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformGlobalRegionGlobalRegionTextDatum.(*[]*DataPlatformGlobalRegionGlobalRegionTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformGlobalRegionGlobalRegionTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformGlobalRegionGlobalRegionTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformGlobalRegionGlobalRegionTextDatumR{}
		}
		args = append(args, object.GlobalRegion)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformGlobalRegionGlobalRegionTextDatumR{}
			}

			for _, a := range args {
				if a == obj.GlobalRegion {
					continue Outer
				}
			}

			args = append(args, obj.GlobalRegion)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_global_region_global_region_data`),
		qm.WhereIn(`data_platform_global_region_global_region_data.GlobalRegion in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformGlobalRegionGlobalRegionDatum")
	}

	var resultSlice []*DataPlatformGlobalRegionGlobalRegionDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformGlobalRegionGlobalRegionDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_global_region_global_region_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_global_region_global_region_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GlobalRegion == foreign.GlobalRegion {
				local.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum = foreign
				break
			}
		}
	}

	return nil
}

// SetGlobalRegionDataPlatformGlobalRegionGlobalRegionDatum of the dataPlatformGlobalRegionGlobalRegionTextDatum to the related item.
// Sets o.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum to related.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) SetGlobalRegionDataPlatformGlobalRegionGlobalRegionDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformGlobalRegionGlobalRegionDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_global_region_global_region_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"GlobalRegion"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.GlobalRegion, o.GlobalRegion, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GlobalRegion = related.GlobalRegion
	if o.R == nil {
		o.R = &dataPlatformGlobalRegionGlobalRegionTextDatumR{
			GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum: related,
		}
	} else {
		o.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionDatum = related
	}

	return nil
}

// DataPlatformGlobalRegionGlobalRegionTextData retrieves all the records using an executor.
func DataPlatformGlobalRegionGlobalRegionTextData(mods ...qm.QueryMod) dataPlatformGlobalRegionGlobalRegionTextDatumQuery {
	mods = append(mods, qm.From("`data_platform_global_region_global_region_text_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_global_region_global_region_text_data`.*"})
	}

	return dataPlatformGlobalRegionGlobalRegionTextDatumQuery{q}
}

// FindDataPlatformGlobalRegionGlobalRegionTextDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformGlobalRegionGlobalRegionTextDatum(ctx context.Context, exec boil.ContextExecutor, globalRegion string, language string, selectCols ...string) (*DataPlatformGlobalRegionGlobalRegionTextDatum, error) {
	dataPlatformGlobalRegionGlobalRegionTextDatumObj := &DataPlatformGlobalRegionGlobalRegionTextDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_global_region_global_region_text_data` where `GlobalRegion`=? AND `Language`=?", sel,
	)

	q := queries.Raw(query, globalRegion, language)

	err := q.Bind(ctx, exec, dataPlatformGlobalRegionGlobalRegionTextDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_global_region_global_region_text_data")
	}

	return dataPlatformGlobalRegionGlobalRegionTextDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_global_region_global_region_text_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformGlobalRegionGlobalRegionTextDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionTextDatumInsertCache[key]
	dataPlatformGlobalRegionGlobalRegionTextDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformGlobalRegionGlobalRegionTextDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithDefault,
			dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_global_region_global_region_text_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_global_region_global_region_text_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_global_region_global_region_text_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_global_region_global_region_text_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GlobalRegion,
		o.Language,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_global_region_global_region_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformGlobalRegionGlobalRegionTextDatumInsertCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionTextDatumInsertCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionTextDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformGlobalRegionGlobalRegionTextDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCache[key]
	dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformGlobalRegionGlobalRegionTextDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_global_region_global_region_text_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_global_region_global_region_text_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, append(wl, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_global_region_global_region_text_data row")
	}

	if !cached {
		dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionTextDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_global_region_global_region_text_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformGlobalRegionGlobalRegionTextDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_global_region_global_region_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformGlobalRegionGlobalRegionTextDatum slice")
	}

	return nil
}

var mySQLDataPlatformGlobalRegionGlobalRegionTextDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_global_region_global_region_text_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformGlobalRegionGlobalRegionTextDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCache[key]
	dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformGlobalRegionGlobalRegionTextDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithDefault,
			dataPlatformGlobalRegionGlobalRegionTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformGlobalRegionGlobalRegionTextDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_global_region_global_region_text_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_global_region_global_region_text_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_global_region_global_region_text_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_global_region_global_region_text_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionTextDatumType, dataPlatformGlobalRegionGlobalRegionTextDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_global_region_global_region_text_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_global_region_global_region_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionTextDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformGlobalRegionGlobalRegionTextDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformGlobalRegionGlobalRegionTextDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_global_region_global_region_text_data` WHERE `GlobalRegion`=? AND `Language`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_global_region_global_region_text_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformGlobalRegionGlobalRegionTextDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformGlobalRegionGlobalRegionTextDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_global_region_global_region_text_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformGlobalRegionGlobalRegionTextDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_global_region_global_region_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformGlobalRegionGlobalRegionTextDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformGlobalRegionGlobalRegionTextDatum(ctx, exec, o.GlobalRegion, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformGlobalRegionGlobalRegionTextDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_global_region_global_region_text_data`.* FROM `data_platform_global_region_global_region_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformGlobalRegionGlobalRegionTextDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformGlobalRegionGlobalRegionTextDatumExists checks if the DataPlatformGlobalRegionGlobalRegionTextDatum row exists.
func DataPlatformGlobalRegionGlobalRegionTextDatumExists(ctx context.Context, exec boil.ContextExecutor, globalRegion string, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_global_region_global_region_text_data` where `GlobalRegion`=? AND `Language`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, globalRegion, language)
	}
	row := exec.QueryRowContext(ctx, sql, globalRegion, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_global_region_global_region_text_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformGlobalRegionGlobalRegionTextDatum row exists.
func (o *DataPlatformGlobalRegionGlobalRegionTextDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformGlobalRegionGlobalRegionTextDatumExists(ctx, exec, o.GlobalRegion, o.Language)
}
