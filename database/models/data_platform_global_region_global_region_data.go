// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformGlobalRegionGlobalRegionDatum is an object representing the database table.
type DataPlatformGlobalRegionGlobalRegionDatum struct {
	GlobalRegion string `boil:"GlobalRegion" json:"GlobalRegion" toml:"GlobalRegion" yaml:"GlobalRegion"`

	R *dataPlatformGlobalRegionGlobalRegionDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformGlobalRegionGlobalRegionDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformGlobalRegionGlobalRegionDatumColumns = struct {
	GlobalRegion string
}{
	GlobalRegion: "GlobalRegion",
}

var DataPlatformGlobalRegionGlobalRegionDatumTableColumns = struct {
	GlobalRegion string
}{
	GlobalRegion: "data_platform_global_region_global_region_data.GlobalRegion",
}

// Generated where

var DataPlatformGlobalRegionGlobalRegionDatumWhere = struct {
	GlobalRegion whereHelperstring
}{
	GlobalRegion: whereHelperstring{field: "`data_platform_global_region_global_region_data`.`GlobalRegion`"},
}

// DataPlatformGlobalRegionGlobalRegionDatumRels is where relationship names are stored.
var DataPlatformGlobalRegionGlobalRegionDatumRels = struct {
	GlobalRegionDataPlatformCountryCountryData               string
	GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData string
}{
	GlobalRegionDataPlatformCountryCountryData:               "GlobalRegionDataPlatformCountryCountryData",
	GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData: "GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData",
}

// dataPlatformGlobalRegionGlobalRegionDatumR is where relationships are stored.
type dataPlatformGlobalRegionGlobalRegionDatumR struct {
	GlobalRegionDataPlatformCountryCountryData               DataPlatformCountryCountryDatumSlice               `boil:"GlobalRegionDataPlatformCountryCountryData" json:"GlobalRegionDataPlatformCountryCountryData" toml:"GlobalRegionDataPlatformCountryCountryData" yaml:"GlobalRegionDataPlatformCountryCountryData"`
	GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData DataPlatformGlobalRegionGlobalRegionTextDatumSlice `boil:"GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData" json:"GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData" toml:"GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData" yaml:"GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformGlobalRegionGlobalRegionDatumR) NewStruct() *dataPlatformGlobalRegionGlobalRegionDatumR {
	return &dataPlatformGlobalRegionGlobalRegionDatumR{}
}

func (r *dataPlatformGlobalRegionGlobalRegionDatumR) GetGlobalRegionDataPlatformCountryCountryData() DataPlatformCountryCountryDatumSlice {
	if r == nil {
		return nil
	}
	return r.GlobalRegionDataPlatformCountryCountryData
}

func (r *dataPlatformGlobalRegionGlobalRegionDatumR) GetGlobalRegionDataPlatformGlobalRegionGlobalRegionTextData() DataPlatformGlobalRegionGlobalRegionTextDatumSlice {
	if r == nil {
		return nil
	}
	return r.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData
}

// dataPlatformGlobalRegionGlobalRegionDatumL is where Load methods for each relationship are stored.
type dataPlatformGlobalRegionGlobalRegionDatumL struct{}

var (
	dataPlatformGlobalRegionGlobalRegionDatumAllColumns            = []string{"GlobalRegion"}
	dataPlatformGlobalRegionGlobalRegionDatumColumnsWithoutDefault = []string{"GlobalRegion"}
	dataPlatformGlobalRegionGlobalRegionDatumColumnsWithDefault    = []string{}
	dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns     = []string{"GlobalRegion"}
	dataPlatformGlobalRegionGlobalRegionDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformGlobalRegionGlobalRegionDatumSlice is an alias for a slice of pointers to DataPlatformGlobalRegionGlobalRegionDatum.
	// This should almost always be used instead of []DataPlatformGlobalRegionGlobalRegionDatum.
	DataPlatformGlobalRegionGlobalRegionDatumSlice []*DataPlatformGlobalRegionGlobalRegionDatum

	dataPlatformGlobalRegionGlobalRegionDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformGlobalRegionGlobalRegionDatumType                 = reflect.TypeOf(&DataPlatformGlobalRegionGlobalRegionDatum{})
	dataPlatformGlobalRegionGlobalRegionDatumMapping              = queries.MakeStructMapping(dataPlatformGlobalRegionGlobalRegionDatumType)
	dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns)
	dataPlatformGlobalRegionGlobalRegionDatumInsertCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionDatumInsertCache          = make(map[string]insertCache)
	dataPlatformGlobalRegionGlobalRegionDatumUpdateCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformGlobalRegionGlobalRegionDatumUpsertCacheMut       sync.RWMutex
	dataPlatformGlobalRegionGlobalRegionDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformGlobalRegionGlobalRegionDatum record from the query.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformGlobalRegionGlobalRegionDatum, error) {
	o := &DataPlatformGlobalRegionGlobalRegionDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_global_region_global_region_data")
	}

	return o, nil
}

// All returns all DataPlatformGlobalRegionGlobalRegionDatum records from the query.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformGlobalRegionGlobalRegionDatumSlice, error) {
	var o []*DataPlatformGlobalRegionGlobalRegionDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformGlobalRegionGlobalRegionDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformGlobalRegionGlobalRegionDatum records in the query.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_global_region_global_region_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_global_region_global_region_data exists")
	}

	return count > 0, nil
}

// GlobalRegionDataPlatformCountryCountryData retrieves all the data_platform_country_country_datum's DataPlatformCountryCountryData with an executor via GlobalRegion column.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) GlobalRegionDataPlatformCountryCountryData(mods ...qm.QueryMod) dataPlatformCountryCountryDatumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`data_platform_country_country_data`.`GlobalRegion`=?", o.GlobalRegion),
	)

	return DataPlatformCountryCountryData(queryMods...)
}

// GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData retrieves all the data_platform_global_region_global_region_text_datum's DataPlatformGlobalRegionGlobalRegionTextData with an executor via GlobalRegion column.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData(mods ...qm.QueryMod) dataPlatformGlobalRegionGlobalRegionTextDatumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`data_platform_global_region_global_region_text_data`.`GlobalRegion`=?", o.GlobalRegion),
	)

	return DataPlatformGlobalRegionGlobalRegionTextData(queryMods...)
}

// LoadGlobalRegionDataPlatformCountryCountryData allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (dataPlatformGlobalRegionGlobalRegionDatumL) LoadGlobalRegionDataPlatformCountryCountryData(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformGlobalRegionGlobalRegionDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformGlobalRegionGlobalRegionDatum
	var object *DataPlatformGlobalRegionGlobalRegionDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformGlobalRegionGlobalRegionDatum.(*DataPlatformGlobalRegionGlobalRegionDatum)
		if !ok {
			object = new(DataPlatformGlobalRegionGlobalRegionDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformGlobalRegionGlobalRegionDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformGlobalRegionGlobalRegionDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformGlobalRegionGlobalRegionDatum.(*[]*DataPlatformGlobalRegionGlobalRegionDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformGlobalRegionGlobalRegionDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformGlobalRegionGlobalRegionDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformGlobalRegionGlobalRegionDatumR{}
		}
		args = append(args, object.GlobalRegion)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformGlobalRegionGlobalRegionDatumR{}
			}

			for _, a := range args {
				if a == obj.GlobalRegion {
					continue Outer
				}
			}

			args = append(args, obj.GlobalRegion)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_country_country_data`),
		qm.WhereIn(`data_platform_country_country_data.GlobalRegion in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load data_platform_country_country_data")
	}

	var resultSlice []*DataPlatformCountryCountryDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice data_platform_country_country_data")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on data_platform_country_country_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_country_country_data")
	}

	if singular {
		object.R.GlobalRegionDataPlatformCountryCountryData = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.GlobalRegion == foreign.GlobalRegion {
				local.R.GlobalRegionDataPlatformCountryCountryData = append(local.R.GlobalRegionDataPlatformCountryCountryData, foreign)
				break
			}
		}
	}

	return nil
}

// LoadGlobalRegionDataPlatformGlobalRegionGlobalRegionTextData allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (dataPlatformGlobalRegionGlobalRegionDatumL) LoadGlobalRegionDataPlatformGlobalRegionGlobalRegionTextData(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformGlobalRegionGlobalRegionDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformGlobalRegionGlobalRegionDatum
	var object *DataPlatformGlobalRegionGlobalRegionDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformGlobalRegionGlobalRegionDatum.(*DataPlatformGlobalRegionGlobalRegionDatum)
		if !ok {
			object = new(DataPlatformGlobalRegionGlobalRegionDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformGlobalRegionGlobalRegionDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformGlobalRegionGlobalRegionDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformGlobalRegionGlobalRegionDatum.(*[]*DataPlatformGlobalRegionGlobalRegionDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformGlobalRegionGlobalRegionDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformGlobalRegionGlobalRegionDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformGlobalRegionGlobalRegionDatumR{}
		}
		args = append(args, object.GlobalRegion)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformGlobalRegionGlobalRegionDatumR{}
			}

			for _, a := range args {
				if a == obj.GlobalRegion {
					continue Outer
				}
			}

			args = append(args, obj.GlobalRegion)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_global_region_global_region_text_data`),
		qm.WhereIn(`data_platform_global_region_global_region_text_data.GlobalRegion in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load data_platform_global_region_global_region_text_data")
	}

	var resultSlice []*DataPlatformGlobalRegionGlobalRegionTextDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice data_platform_global_region_global_region_text_data")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on data_platform_global_region_global_region_text_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_global_region_global_region_text_data")
	}

	if singular {
		object.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.GlobalRegion == foreign.GlobalRegion {
				local.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData = append(local.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData, foreign)
				break
			}
		}
	}

	return nil
}

// AddGlobalRegionDataPlatformCountryCountryData adds the given related objects to the existing relationships
// of the data_platform_global_region_global_region_datum, optionally inserting them as new records.
// Appends related to o.R.GlobalRegionDataPlatformCountryCountryData.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) AddGlobalRegionDataPlatformCountryCountryData(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DataPlatformCountryCountryDatum) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GlobalRegion = o.GlobalRegion
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `data_platform_country_country_data` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"GlobalRegion"}),
				strmangle.WhereClause("`", "`", 0, dataPlatformCountryCountryDatumPrimaryKeyColumns),
			)
			values := []interface{}{o.GlobalRegion, rel.Country}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GlobalRegion = o.GlobalRegion
		}
	}

	if o.R == nil {
		o.R = &dataPlatformGlobalRegionGlobalRegionDatumR{
			GlobalRegionDataPlatformCountryCountryData: related,
		}
	} else {
		o.R.GlobalRegionDataPlatformCountryCountryData = append(o.R.GlobalRegionDataPlatformCountryCountryData, related...)
	}

	return nil
}

// AddGlobalRegionDataPlatformGlobalRegionGlobalRegionTextData adds the given related objects to the existing relationships
// of the data_platform_global_region_global_region_datum, optionally inserting them as new records.
// Appends related to o.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) AddGlobalRegionDataPlatformGlobalRegionGlobalRegionTextData(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DataPlatformGlobalRegionGlobalRegionTextDatum) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GlobalRegion = o.GlobalRegion
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `data_platform_global_region_global_region_text_data` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"GlobalRegion"}),
				strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionTextDatumPrimaryKeyColumns),
			)
			values := []interface{}{o.GlobalRegion, rel.GlobalRegion, rel.Language}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GlobalRegion = o.GlobalRegion
		}
	}

	if o.R == nil {
		o.R = &dataPlatformGlobalRegionGlobalRegionDatumR{
			GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData: related,
		}
	} else {
		o.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData = append(o.R.GlobalRegionDataPlatformGlobalRegionGlobalRegionTextData, related...)
	}

	return nil
}

// DataPlatformGlobalRegionGlobalRegionData retrieves all the records using an executor.
func DataPlatformGlobalRegionGlobalRegionData(mods ...qm.QueryMod) dataPlatformGlobalRegionGlobalRegionDatumQuery {
	mods = append(mods, qm.From("`data_platform_global_region_global_region_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_global_region_global_region_data`.*"})
	}

	return dataPlatformGlobalRegionGlobalRegionDatumQuery{q}
}

// FindDataPlatformGlobalRegionGlobalRegionDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformGlobalRegionGlobalRegionDatum(ctx context.Context, exec boil.ContextExecutor, globalRegion string, selectCols ...string) (*DataPlatformGlobalRegionGlobalRegionDatum, error) {
	dataPlatformGlobalRegionGlobalRegionDatumObj := &DataPlatformGlobalRegionGlobalRegionDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_global_region_global_region_data` where `GlobalRegion`=?", sel,
	)

	q := queries.Raw(query, globalRegion)

	err := q.Bind(ctx, exec, dataPlatformGlobalRegionGlobalRegionDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_global_region_global_region_data")
	}

	return dataPlatformGlobalRegionGlobalRegionDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_global_region_global_region_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformGlobalRegionGlobalRegionDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformGlobalRegionGlobalRegionDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionDatumInsertCache[key]
	dataPlatformGlobalRegionGlobalRegionDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformGlobalRegionGlobalRegionDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionDatumColumnsWithDefault,
			dataPlatformGlobalRegionGlobalRegionDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_global_region_global_region_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_global_region_global_region_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_global_region_global_region_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_global_region_global_region_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.GlobalRegion,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_global_region_global_region_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformGlobalRegionGlobalRegionDatumInsertCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionDatumInsertCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformGlobalRegionGlobalRegionDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformGlobalRegionGlobalRegionDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionDatumUpdateCache[key]
	dataPlatformGlobalRegionGlobalRegionDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformGlobalRegionGlobalRegionDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_global_region_global_region_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_global_region_global_region_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, append(wl, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_global_region_global_region_data row")
	}

	if !cached {
		dataPlatformGlobalRegionGlobalRegionDatumUpdateCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionDatumUpdateCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_global_region_global_region_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformGlobalRegionGlobalRegionDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_global_region_global_region_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformGlobalRegionGlobalRegionDatum slice")
	}

	return nil
}

var mySQLDataPlatformGlobalRegionGlobalRegionDatumUniqueColumns = []string{
	"GlobalRegion",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_global_region_global_region_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformGlobalRegionGlobalRegionDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformGlobalRegionGlobalRegionDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformGlobalRegionGlobalRegionDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformGlobalRegionGlobalRegionDatumUpsertCache[key]
	dataPlatformGlobalRegionGlobalRegionDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformGlobalRegionGlobalRegionDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionDatumColumnsWithDefault,
			dataPlatformGlobalRegionGlobalRegionDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformGlobalRegionGlobalRegionDatumAllColumns,
			dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_global_region_global_region_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_global_region_global_region_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_global_region_global_region_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_global_region_global_region_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformGlobalRegionGlobalRegionDatumType, dataPlatformGlobalRegionGlobalRegionDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_global_region_global_region_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_global_region_global_region_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformGlobalRegionGlobalRegionDatumUpsertCacheMut.Lock()
		dataPlatformGlobalRegionGlobalRegionDatumUpsertCache[key] = cache
		dataPlatformGlobalRegionGlobalRegionDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformGlobalRegionGlobalRegionDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformGlobalRegionGlobalRegionDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_global_region_global_region_data` WHERE `GlobalRegion`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_global_region_global_region_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformGlobalRegionGlobalRegionDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformGlobalRegionGlobalRegionDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_global_region_global_region_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformGlobalRegionGlobalRegionDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_global_region_global_region_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformGlobalRegionGlobalRegionDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformGlobalRegionGlobalRegionDatum(ctx, exec, o.GlobalRegion)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformGlobalRegionGlobalRegionDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformGlobalRegionGlobalRegionDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_global_region_global_region_data`.* FROM `data_platform_global_region_global_region_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformGlobalRegionGlobalRegionDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformGlobalRegionGlobalRegionDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformGlobalRegionGlobalRegionDatumExists checks if the DataPlatformGlobalRegionGlobalRegionDatum row exists.
func DataPlatformGlobalRegionGlobalRegionDatumExists(ctx context.Context, exec boil.ContextExecutor, globalRegion string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_global_region_global_region_data` where `GlobalRegion`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, globalRegion)
	}
	row := exec.QueryRowContext(ctx, sql, globalRegion)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_global_region_global_region_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformGlobalRegionGlobalRegionDatum row exists.
func (o *DataPlatformGlobalRegionGlobalRegionDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformGlobalRegionGlobalRegionDatumExists(ctx, exec, o.GlobalRegion)
}
