// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformProductMasterAllergenDatum is an object representing the database table.
type DataPlatformProductMasterAllergenDatum struct {
	Product             string    `boil:"Product" json:"Product" toml:"Product" yaml:"Product"`
	BusinessPartner     int       `boil:"BusinessPartner" json:"BusinessPartner" toml:"BusinessPartner" yaml:"BusinessPartner"`
	Allergen            string    `boil:"Allergen" json:"Allergen" toml:"Allergen" yaml:"Allergen"`
	AllergenIsContained null.Bool `boil:"AllergenIsContained" json:"AllergenIsContained,omitempty" toml:"AllergenIsContained" yaml:"AllergenIsContained,omitempty"`
	IsMarkedForDeletion null.Bool `boil:"IsMarkedForDeletion" json:"IsMarkedForDeletion,omitempty" toml:"IsMarkedForDeletion" yaml:"IsMarkedForDeletion,omitempty"`

	R *dataPlatformProductMasterAllergenDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformProductMasterAllergenDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformProductMasterAllergenDatumColumns = struct {
	Product             string
	BusinessPartner     string
	Allergen            string
	AllergenIsContained string
	IsMarkedForDeletion string
}{
	Product:             "Product",
	BusinessPartner:     "BusinessPartner",
	Allergen:            "Allergen",
	AllergenIsContained: "AllergenIsContained",
	IsMarkedForDeletion: "IsMarkedForDeletion",
}

var DataPlatformProductMasterAllergenDatumTableColumns = struct {
	Product             string
	BusinessPartner     string
	Allergen            string
	AllergenIsContained string
	IsMarkedForDeletion string
}{
	Product:             "data_platform_product_master_allergen_data.Product",
	BusinessPartner:     "data_platform_product_master_allergen_data.BusinessPartner",
	Allergen:            "data_platform_product_master_allergen_data.Allergen",
	AllergenIsContained: "data_platform_product_master_allergen_data.AllergenIsContained",
	IsMarkedForDeletion: "data_platform_product_master_allergen_data.IsMarkedForDeletion",
}

// Generated where

var DataPlatformProductMasterAllergenDatumWhere = struct {
	Product             whereHelperstring
	BusinessPartner     whereHelperint
	Allergen            whereHelperstring
	AllergenIsContained whereHelpernull_Bool
	IsMarkedForDeletion whereHelpernull_Bool
}{
	Product:             whereHelperstring{field: "`data_platform_product_master_allergen_data`.`Product`"},
	BusinessPartner:     whereHelperint{field: "`data_platform_product_master_allergen_data`.`BusinessPartner`"},
	Allergen:            whereHelperstring{field: "`data_platform_product_master_allergen_data`.`Allergen`"},
	AllergenIsContained: whereHelpernull_Bool{field: "`data_platform_product_master_allergen_data`.`AllergenIsContained`"},
	IsMarkedForDeletion: whereHelpernull_Bool{field: "`data_platform_product_master_allergen_data`.`IsMarkedForDeletion`"},
}

// DataPlatformProductMasterAllergenDatumRels is where relationship names are stored.
var DataPlatformProductMasterAllergenDatumRels = struct {
}{}

// dataPlatformProductMasterAllergenDatumR is where relationships are stored.
type dataPlatformProductMasterAllergenDatumR struct {
}

// NewStruct creates a new relationship struct
func (*dataPlatformProductMasterAllergenDatumR) NewStruct() *dataPlatformProductMasterAllergenDatumR {
	return &dataPlatformProductMasterAllergenDatumR{}
}

// dataPlatformProductMasterAllergenDatumL is where Load methods for each relationship are stored.
type dataPlatformProductMasterAllergenDatumL struct{}

var (
	dataPlatformProductMasterAllergenDatumAllColumns            = []string{"Product", "BusinessPartner", "Allergen", "AllergenIsContained", "IsMarkedForDeletion"}
	dataPlatformProductMasterAllergenDatumColumnsWithoutDefault = []string{"Product", "BusinessPartner", "Allergen", "AllergenIsContained", "IsMarkedForDeletion"}
	dataPlatformProductMasterAllergenDatumColumnsWithDefault    = []string{}
	dataPlatformProductMasterAllergenDatumPrimaryKeyColumns     = []string{"Product", "BusinessPartner", "Allergen"}
	dataPlatformProductMasterAllergenDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformProductMasterAllergenDatumSlice is an alias for a slice of pointers to DataPlatformProductMasterAllergenDatum.
	// This should almost always be used instead of []DataPlatformProductMasterAllergenDatum.
	DataPlatformProductMasterAllergenDatumSlice []*DataPlatformProductMasterAllergenDatum

	dataPlatformProductMasterAllergenDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformProductMasterAllergenDatumType                 = reflect.TypeOf(&DataPlatformProductMasterAllergenDatum{})
	dataPlatformProductMasterAllergenDatumMapping              = queries.MakeStructMapping(dataPlatformProductMasterAllergenDatumType)
	dataPlatformProductMasterAllergenDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns)
	dataPlatformProductMasterAllergenDatumInsertCacheMut       sync.RWMutex
	dataPlatformProductMasterAllergenDatumInsertCache          = make(map[string]insertCache)
	dataPlatformProductMasterAllergenDatumUpdateCacheMut       sync.RWMutex
	dataPlatformProductMasterAllergenDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformProductMasterAllergenDatumUpsertCacheMut       sync.RWMutex
	dataPlatformProductMasterAllergenDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformProductMasterAllergenDatum record from the query.
func (q dataPlatformProductMasterAllergenDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformProductMasterAllergenDatum, error) {
	o := &DataPlatformProductMasterAllergenDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_product_master_allergen_data")
	}

	return o, nil
}

// All returns all DataPlatformProductMasterAllergenDatum records from the query.
func (q dataPlatformProductMasterAllergenDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformProductMasterAllergenDatumSlice, error) {
	var o []*DataPlatformProductMasterAllergenDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformProductMasterAllergenDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformProductMasterAllergenDatum records in the query.
func (q dataPlatformProductMasterAllergenDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_product_master_allergen_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformProductMasterAllergenDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_product_master_allergen_data exists")
	}

	return count > 0, nil
}

// DataPlatformProductMasterAllergenData retrieves all the records using an executor.
func DataPlatformProductMasterAllergenData(mods ...qm.QueryMod) dataPlatformProductMasterAllergenDatumQuery {
	mods = append(mods, qm.From("`data_platform_product_master_allergen_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_product_master_allergen_data`.*"})
	}

	return dataPlatformProductMasterAllergenDatumQuery{q}
}

// FindDataPlatformProductMasterAllergenDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformProductMasterAllergenDatum(ctx context.Context, exec boil.ContextExecutor, product string, businessPartner int, allergen string, selectCols ...string) (*DataPlatformProductMasterAllergenDatum, error) {
	dataPlatformProductMasterAllergenDatumObj := &DataPlatformProductMasterAllergenDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_product_master_allergen_data` where `Product`=? AND `BusinessPartner`=? AND `Allergen`=?", sel,
	)

	q := queries.Raw(query, product, businessPartner, allergen)

	err := q.Bind(ctx, exec, dataPlatformProductMasterAllergenDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_product_master_allergen_data")
	}

	return dataPlatformProductMasterAllergenDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformProductMasterAllergenDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_product_master_allergen_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformProductMasterAllergenDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformProductMasterAllergenDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformProductMasterAllergenDatumInsertCache[key]
	dataPlatformProductMasterAllergenDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformProductMasterAllergenDatumAllColumns,
			dataPlatformProductMasterAllergenDatumColumnsWithDefault,
			dataPlatformProductMasterAllergenDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_product_master_allergen_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_product_master_allergen_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_product_master_allergen_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_product_master_allergen_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.Product,
		o.BusinessPartner,
		o.Allergen,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_product_master_allergen_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformProductMasterAllergenDatumInsertCacheMut.Lock()
		dataPlatformProductMasterAllergenDatumInsertCache[key] = cache
		dataPlatformProductMasterAllergenDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformProductMasterAllergenDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformProductMasterAllergenDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformProductMasterAllergenDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformProductMasterAllergenDatumUpdateCache[key]
	dataPlatformProductMasterAllergenDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformProductMasterAllergenDatumAllColumns,
			dataPlatformProductMasterAllergenDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_product_master_allergen_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_product_master_allergen_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, append(wl, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_product_master_allergen_data row")
	}

	if !cached {
		dataPlatformProductMasterAllergenDatumUpdateCacheMut.Lock()
		dataPlatformProductMasterAllergenDatumUpdateCache[key] = cache
		dataPlatformProductMasterAllergenDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformProductMasterAllergenDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_product_master_allergen_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformProductMasterAllergenDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformProductMasterAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_product_master_allergen_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformProductMasterAllergenDatum slice")
	}

	return nil
}

var mySQLDataPlatformProductMasterAllergenDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformProductMasterAllergenDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_product_master_allergen_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformProductMasterAllergenDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformProductMasterAllergenDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformProductMasterAllergenDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformProductMasterAllergenDatumUpsertCache[key]
	dataPlatformProductMasterAllergenDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformProductMasterAllergenDatumAllColumns,
			dataPlatformProductMasterAllergenDatumColumnsWithDefault,
			dataPlatformProductMasterAllergenDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformProductMasterAllergenDatumAllColumns,
			dataPlatformProductMasterAllergenDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_product_master_allergen_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_product_master_allergen_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_product_master_allergen_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_product_master_allergen_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformProductMasterAllergenDatumType, dataPlatformProductMasterAllergenDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_product_master_allergen_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_product_master_allergen_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformProductMasterAllergenDatumUpsertCacheMut.Lock()
		dataPlatformProductMasterAllergenDatumUpsertCache[key] = cache
		dataPlatformProductMasterAllergenDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformProductMasterAllergenDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformProductMasterAllergenDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformProductMasterAllergenDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformProductMasterAllergenDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_product_master_allergen_data` WHERE `Product`=? AND `BusinessPartner`=? AND `Allergen`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_product_master_allergen_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformProductMasterAllergenDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformProductMasterAllergenDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_product_master_allergen_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformProductMasterAllergenDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformProductMasterAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_product_master_allergen_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformProductMasterAllergenDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformProductMasterAllergenDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformProductMasterAllergenDatum(ctx, exec, o.Product, o.BusinessPartner, o.Allergen)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformProductMasterAllergenDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformProductMasterAllergenDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformProductMasterAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_product_master_allergen_data`.* FROM `data_platform_product_master_allergen_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformProductMasterAllergenDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformProductMasterAllergenDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformProductMasterAllergenDatumExists checks if the DataPlatformProductMasterAllergenDatum row exists.
func DataPlatformProductMasterAllergenDatumExists(ctx context.Context, exec boil.ContextExecutor, product string, businessPartner int, allergen string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_product_master_allergen_data` where `Product`=? AND `BusinessPartner`=? AND `Allergen`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, product, businessPartner, allergen)
	}
	row := exec.QueryRowContext(ctx, sql, product, businessPartner, allergen)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_product_master_allergen_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformProductMasterAllergenDatum row exists.
func (o *DataPlatformProductMasterAllergenDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformProductMasterAllergenDatumExists(ctx, exec, o.Product, o.BusinessPartner, o.Allergen)
}
