// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformAllergenAllergenDatum is an object representing the database table.
type DataPlatformAllergenAllergenDatum struct {
	Allergen string `boil:"Allergen" json:"Allergen" toml:"Allergen" yaml:"Allergen"`

	R *dataPlatformAllergenAllergenDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformAllergenAllergenDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformAllergenAllergenDatumColumns = struct {
	Allergen string
}{
	Allergen: "Allergen",
}

var DataPlatformAllergenAllergenDatumTableColumns = struct {
	Allergen string
}{
	Allergen: "data_platform_allergen_allergen_data.Allergen",
}

// Generated where

var DataPlatformAllergenAllergenDatumWhere = struct {
	Allergen whereHelperstring
}{
	Allergen: whereHelperstring{field: "`data_platform_allergen_allergen_data`.`Allergen`"},
}

// DataPlatformAllergenAllergenDatumRels is where relationship names are stored.
var DataPlatformAllergenAllergenDatumRels = struct {
	AllergenDataPlatformAllergenAllergenTextData string
}{
	AllergenDataPlatformAllergenAllergenTextData: "AllergenDataPlatformAllergenAllergenTextData",
}

// dataPlatformAllergenAllergenDatumR is where relationships are stored.
type dataPlatformAllergenAllergenDatumR struct {
	AllergenDataPlatformAllergenAllergenTextData DataPlatformAllergenAllergenTextDatumSlice `boil:"AllergenDataPlatformAllergenAllergenTextData" json:"AllergenDataPlatformAllergenAllergenTextData" toml:"AllergenDataPlatformAllergenAllergenTextData" yaml:"AllergenDataPlatformAllergenAllergenTextData"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformAllergenAllergenDatumR) NewStruct() *dataPlatformAllergenAllergenDatumR {
	return &dataPlatformAllergenAllergenDatumR{}
}

func (r *dataPlatformAllergenAllergenDatumR) GetAllergenDataPlatformAllergenAllergenTextData() DataPlatformAllergenAllergenTextDatumSlice {
	if r == nil {
		return nil
	}
	return r.AllergenDataPlatformAllergenAllergenTextData
}

// dataPlatformAllergenAllergenDatumL is where Load methods for each relationship are stored.
type dataPlatformAllergenAllergenDatumL struct{}

var (
	dataPlatformAllergenAllergenDatumAllColumns            = []string{"Allergen"}
	dataPlatformAllergenAllergenDatumColumnsWithoutDefault = []string{"Allergen"}
	dataPlatformAllergenAllergenDatumColumnsWithDefault    = []string{}
	dataPlatformAllergenAllergenDatumPrimaryKeyColumns     = []string{"Allergen"}
	dataPlatformAllergenAllergenDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformAllergenAllergenDatumSlice is an alias for a slice of pointers to DataPlatformAllergenAllergenDatum.
	// This should almost always be used instead of []DataPlatformAllergenAllergenDatum.
	DataPlatformAllergenAllergenDatumSlice []*DataPlatformAllergenAllergenDatum

	dataPlatformAllergenAllergenDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformAllergenAllergenDatumType                 = reflect.TypeOf(&DataPlatformAllergenAllergenDatum{})
	dataPlatformAllergenAllergenDatumMapping              = queries.MakeStructMapping(dataPlatformAllergenAllergenDatumType)
	dataPlatformAllergenAllergenDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, dataPlatformAllergenAllergenDatumPrimaryKeyColumns)
	dataPlatformAllergenAllergenDatumInsertCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenDatumInsertCache          = make(map[string]insertCache)
	dataPlatformAllergenAllergenDatumUpdateCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformAllergenAllergenDatumUpsertCacheMut       sync.RWMutex
	dataPlatformAllergenAllergenDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformAllergenAllergenDatum record from the query.
func (q dataPlatformAllergenAllergenDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformAllergenAllergenDatum, error) {
	o := &DataPlatformAllergenAllergenDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_allergen_allergen_data")
	}

	return o, nil
}

// All returns all DataPlatformAllergenAllergenDatum records from the query.
func (q dataPlatformAllergenAllergenDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformAllergenAllergenDatumSlice, error) {
	var o []*DataPlatformAllergenAllergenDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformAllergenAllergenDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformAllergenAllergenDatum records in the query.
func (q dataPlatformAllergenAllergenDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_allergen_allergen_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformAllergenAllergenDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_allergen_allergen_data exists")
	}

	return count > 0, nil
}

// AllergenDataPlatformAllergenAllergenTextData retrieves all the data_platform_allergen_allergen_text_datum's DataPlatformAllergenAllergenTextData with an executor via Allergen column.
func (o *DataPlatformAllergenAllergenDatum) AllergenDataPlatformAllergenAllergenTextData(mods ...qm.QueryMod) dataPlatformAllergenAllergenTextDatumQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("`data_platform_allergen_allergen_text_data`.`Allergen`=?", o.Allergen),
	)

	return DataPlatformAllergenAllergenTextData(queryMods...)
}

// LoadAllergenDataPlatformAllergenAllergenTextData allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (dataPlatformAllergenAllergenDatumL) LoadAllergenDataPlatformAllergenAllergenTextData(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformAllergenAllergenDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformAllergenAllergenDatum
	var object *DataPlatformAllergenAllergenDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformAllergenAllergenDatum.(*DataPlatformAllergenAllergenDatum)
		if !ok {
			object = new(DataPlatformAllergenAllergenDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformAllergenAllergenDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformAllergenAllergenDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformAllergenAllergenDatum.(*[]*DataPlatformAllergenAllergenDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformAllergenAllergenDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformAllergenAllergenDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformAllergenAllergenDatumR{}
		}
		args = append(args, object.Allergen)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformAllergenAllergenDatumR{}
			}

			for _, a := range args {
				if a == obj.Allergen {
					continue Outer
				}
			}

			args = append(args, obj.Allergen)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_allergen_allergen_text_data`),
		qm.WhereIn(`data_platform_allergen_allergen_text_data.Allergen in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load data_platform_allergen_allergen_text_data")
	}

	var resultSlice []*DataPlatformAllergenAllergenTextDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice data_platform_allergen_allergen_text_data")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on data_platform_allergen_allergen_text_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_allergen_allergen_text_data")
	}

	if singular {
		object.R.AllergenDataPlatformAllergenAllergenTextData = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.Allergen == foreign.Allergen {
				local.R.AllergenDataPlatformAllergenAllergenTextData = append(local.R.AllergenDataPlatformAllergenAllergenTextData, foreign)
				break
			}
		}
	}

	return nil
}

// AddAllergenDataPlatformAllergenAllergenTextData adds the given related objects to the existing relationships
// of the data_platform_allergen_allergen_datum, optionally inserting them as new records.
// Appends related to o.R.AllergenDataPlatformAllergenAllergenTextData.
func (o *DataPlatformAllergenAllergenDatum) AddAllergenDataPlatformAllergenAllergenTextData(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DataPlatformAllergenAllergenTextDatum) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.Allergen = o.Allergen
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE `data_platform_allergen_allergen_text_data` SET %s WHERE %s",
				strmangle.SetParamNames("`", "`", 0, []string{"Allergen"}),
				strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenTextDatumPrimaryKeyColumns),
			)
			values := []interface{}{o.Allergen, rel.Allergen, rel.Language}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.Allergen = o.Allergen
		}
	}

	if o.R == nil {
		o.R = &dataPlatformAllergenAllergenDatumR{
			AllergenDataPlatformAllergenAllergenTextData: related,
		}
	} else {
		o.R.AllergenDataPlatformAllergenAllergenTextData = append(o.R.AllergenDataPlatformAllergenAllergenTextData, related...)
	}

	return nil
}

// DataPlatformAllergenAllergenData retrieves all the records using an executor.
func DataPlatformAllergenAllergenData(mods ...qm.QueryMod) dataPlatformAllergenAllergenDatumQuery {
	mods = append(mods, qm.From("`data_platform_allergen_allergen_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_allergen_allergen_data`.*"})
	}

	return dataPlatformAllergenAllergenDatumQuery{q}
}

// FindDataPlatformAllergenAllergenDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformAllergenAllergenDatum(ctx context.Context, exec boil.ContextExecutor, allergen string, selectCols ...string) (*DataPlatformAllergenAllergenDatum, error) {
	dataPlatformAllergenAllergenDatumObj := &DataPlatformAllergenAllergenDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_allergen_allergen_data` where `Allergen`=?", sel,
	)

	q := queries.Raw(query, allergen)

	err := q.Bind(ctx, exec, dataPlatformAllergenAllergenDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_allergen_allergen_data")
	}

	return dataPlatformAllergenAllergenDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformAllergenAllergenDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_allergen_allergen_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformAllergenAllergenDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformAllergenAllergenDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenDatumInsertCache[key]
	dataPlatformAllergenAllergenDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformAllergenAllergenDatumAllColumns,
			dataPlatformAllergenAllergenDatumColumnsWithDefault,
			dataPlatformAllergenAllergenDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_allergen_allergen_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_allergen_allergen_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_allergen_allergen_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_allergen_allergen_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.Allergen,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_allergen_allergen_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformAllergenAllergenDatumInsertCacheMut.Lock()
		dataPlatformAllergenAllergenDatumInsertCache[key] = cache
		dataPlatformAllergenAllergenDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformAllergenAllergenDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformAllergenAllergenDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformAllergenAllergenDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenDatumUpdateCache[key]
	dataPlatformAllergenAllergenDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformAllergenAllergenDatumAllColumns,
			dataPlatformAllergenAllergenDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_allergen_allergen_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_allergen_allergen_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformAllergenAllergenDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, append(wl, dataPlatformAllergenAllergenDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_allergen_allergen_data row")
	}

	if !cached {
		dataPlatformAllergenAllergenDatumUpdateCacheMut.Lock()
		dataPlatformAllergenAllergenDatumUpdateCache[key] = cache
		dataPlatformAllergenAllergenDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformAllergenAllergenDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_allergen_allergen_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformAllergenAllergenDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_allergen_allergen_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformAllergenAllergenDatum slice")
	}

	return nil
}

var mySQLDataPlatformAllergenAllergenDatumUniqueColumns = []string{
	"Allergen",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformAllergenAllergenDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_allergen_allergen_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformAllergenAllergenDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformAllergenAllergenDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformAllergenAllergenDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformAllergenAllergenDatumUpsertCache[key]
	dataPlatformAllergenAllergenDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformAllergenAllergenDatumAllColumns,
			dataPlatformAllergenAllergenDatumColumnsWithDefault,
			dataPlatformAllergenAllergenDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformAllergenAllergenDatumAllColumns,
			dataPlatformAllergenAllergenDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_allergen_allergen_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_allergen_allergen_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_allergen_allergen_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_allergen_allergen_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformAllergenAllergenDatumType, dataPlatformAllergenAllergenDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_allergen_allergen_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_allergen_allergen_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformAllergenAllergenDatumUpsertCacheMut.Lock()
		dataPlatformAllergenAllergenDatumUpsertCache[key] = cache
		dataPlatformAllergenAllergenDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformAllergenAllergenDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformAllergenAllergenDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformAllergenAllergenDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformAllergenAllergenDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_allergen_allergen_data` WHERE `Allergen`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_allergen_allergen_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformAllergenAllergenDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformAllergenAllergenDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_allergen_allergen_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformAllergenAllergenDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_allergen_allergen_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformAllergenAllergenDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformAllergenAllergenDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformAllergenAllergenDatum(ctx, exec, o.Allergen)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformAllergenAllergenDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformAllergenAllergenDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformAllergenAllergenDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_allergen_allergen_data`.* FROM `data_platform_allergen_allergen_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformAllergenAllergenDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformAllergenAllergenDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformAllergenAllergenDatumExists checks if the DataPlatformAllergenAllergenDatum row exists.
func DataPlatformAllergenAllergenDatumExists(ctx context.Context, exec boil.ContextExecutor, allergen string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_allergen_allergen_data` where `Allergen`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, allergen)
	}
	row := exec.QueryRowContext(ctx, sql, allergen)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_allergen_allergen_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformAllergenAllergenDatum row exists.
func (o *DataPlatformAllergenAllergenDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformAllergenAllergenDatumExists(ctx, exec, o.Allergen)
}
