// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformCountryCountryTextDatum is an object representing the database table.
type DataPlatformCountryCountryTextDatum struct {
	Country     string `boil:"Country" json:"Country" toml:"Country" yaml:"Country"`
	Language    string `boil:"Language" json:"Language" toml:"Language" yaml:"Language"`
	CountryName string `boil:"CountryName" json:"CountryName" toml:"CountryName" yaml:"CountryName"`

	R *dataPlatformCountryCountryTextDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformCountryCountryTextDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformCountryCountryTextDatumColumns = struct {
	Country     string
	Language    string
	CountryName string
}{
	Country:     "Country",
	Language:    "Language",
	CountryName: "CountryName",
}

var DataPlatformCountryCountryTextDatumTableColumns = struct {
	Country     string
	Language    string
	CountryName string
}{
	Country:     "data_platform_country_country_text_data.Country",
	Language:    "data_platform_country_country_text_data.Language",
	CountryName: "data_platform_country_country_text_data.CountryName",
}

// Generated where

var DataPlatformCountryCountryTextDatumWhere = struct {
	Country     whereHelperstring
	Language    whereHelperstring
	CountryName whereHelperstring
}{
	Country:     whereHelperstring{field: "`data_platform_country_country_text_data`.`Country`"},
	Language:    whereHelperstring{field: "`data_platform_country_country_text_data`.`Language`"},
	CountryName: whereHelperstring{field: "`data_platform_country_country_text_data`.`CountryName`"},
}

// DataPlatformCountryCountryTextDatumRels is where relationship names are stored.
var DataPlatformCountryCountryTextDatumRels = struct {
	LanguageDataPlatformLanguageLanguageDatum string
}{
	LanguageDataPlatformLanguageLanguageDatum: "LanguageDataPlatformLanguageLanguageDatum",
}

// dataPlatformCountryCountryTextDatumR is where relationships are stored.
type dataPlatformCountryCountryTextDatumR struct {
	LanguageDataPlatformLanguageLanguageDatum *DataPlatformLanguageLanguageDatum `boil:"LanguageDataPlatformLanguageLanguageDatum" json:"LanguageDataPlatformLanguageLanguageDatum" toml:"LanguageDataPlatformLanguageLanguageDatum" yaml:"LanguageDataPlatformLanguageLanguageDatum"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformCountryCountryTextDatumR) NewStruct() *dataPlatformCountryCountryTextDatumR {
	return &dataPlatformCountryCountryTextDatumR{}
}

func (r *dataPlatformCountryCountryTextDatumR) GetLanguageDataPlatformLanguageLanguageDatum() *DataPlatformLanguageLanguageDatum {
	if r == nil {
		return nil
	}
	return r.LanguageDataPlatformLanguageLanguageDatum
}

// dataPlatformCountryCountryTextDatumL is where Load methods for each relationship are stored.
type dataPlatformCountryCountryTextDatumL struct{}

var (
	dataPlatformCountryCountryTextDatumAllColumns            = []string{"Country", "Language", "CountryName"}
	dataPlatformCountryCountryTextDatumColumnsWithoutDefault = []string{"Country", "Language", "CountryName"}
	dataPlatformCountryCountryTextDatumColumnsWithDefault    = []string{}
	dataPlatformCountryCountryTextDatumPrimaryKeyColumns     = []string{"Country", "Language"}
	dataPlatformCountryCountryTextDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformCountryCountryTextDatumSlice is an alias for a slice of pointers to DataPlatformCountryCountryTextDatum.
	// This should almost always be used instead of []DataPlatformCountryCountryTextDatum.
	DataPlatformCountryCountryTextDatumSlice []*DataPlatformCountryCountryTextDatum

	dataPlatformCountryCountryTextDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformCountryCountryTextDatumType                 = reflect.TypeOf(&DataPlatformCountryCountryTextDatum{})
	dataPlatformCountryCountryTextDatumMapping              = queries.MakeStructMapping(dataPlatformCountryCountryTextDatumType)
	dataPlatformCountryCountryTextDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, dataPlatformCountryCountryTextDatumPrimaryKeyColumns)
	dataPlatformCountryCountryTextDatumInsertCacheMut       sync.RWMutex
	dataPlatformCountryCountryTextDatumInsertCache          = make(map[string]insertCache)
	dataPlatformCountryCountryTextDatumUpdateCacheMut       sync.RWMutex
	dataPlatformCountryCountryTextDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformCountryCountryTextDatumUpsertCacheMut       sync.RWMutex
	dataPlatformCountryCountryTextDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformCountryCountryTextDatum record from the query.
func (q dataPlatformCountryCountryTextDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformCountryCountryTextDatum, error) {
	o := &DataPlatformCountryCountryTextDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_country_country_text_data")
	}

	return o, nil
}

// All returns all DataPlatformCountryCountryTextDatum records from the query.
func (q dataPlatformCountryCountryTextDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformCountryCountryTextDatumSlice, error) {
	var o []*DataPlatformCountryCountryTextDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformCountryCountryTextDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformCountryCountryTextDatum records in the query.
func (q dataPlatformCountryCountryTextDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_country_country_text_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformCountryCountryTextDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_country_country_text_data exists")
	}

	return count > 0, nil
}

// LanguageDataPlatformLanguageLanguageDatum pointed to by the foreign key.
func (o *DataPlatformCountryCountryTextDatum) LanguageDataPlatformLanguageLanguageDatum(mods ...qm.QueryMod) dataPlatformLanguageLanguageDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`Language` = ?", o.Language),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformLanguageLanguageData(queryMods...)
}

// LoadLanguageDataPlatformLanguageLanguageDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformCountryCountryTextDatumL) LoadLanguageDataPlatformLanguageLanguageDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformCountryCountryTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformCountryCountryTextDatum
	var object *DataPlatformCountryCountryTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformCountryCountryTextDatum.(*DataPlatformCountryCountryTextDatum)
		if !ok {
			object = new(DataPlatformCountryCountryTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformCountryCountryTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformCountryCountryTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformCountryCountryTextDatum.(*[]*DataPlatformCountryCountryTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformCountryCountryTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformCountryCountryTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformCountryCountryTextDatumR{}
		}
		args = append(args, object.Language)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformCountryCountryTextDatumR{}
			}

			for _, a := range args {
				if a == obj.Language {
					continue Outer
				}
			}

			args = append(args, obj.Language)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_language_language_data`),
		qm.WhereIn(`data_platform_language_language_data.Language in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformLanguageLanguageDatum")
	}

	var resultSlice []*DataPlatformLanguageLanguageDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformLanguageLanguageDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_language_language_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_language_language_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.LanguageDataPlatformLanguageLanguageDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Language == foreign.Language {
				local.R.LanguageDataPlatformLanguageLanguageDatum = foreign
				break
			}
		}
	}

	return nil
}

// SetLanguageDataPlatformLanguageLanguageDatum of the dataPlatformCountryCountryTextDatum to the related item.
// Sets o.R.LanguageDataPlatformLanguageLanguageDatum to related.
func (o *DataPlatformCountryCountryTextDatum) SetLanguageDataPlatformLanguageLanguageDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformLanguageLanguageDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_country_country_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"Language"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.Language, o.Country, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Language = related.Language
	if o.R == nil {
		o.R = &dataPlatformCountryCountryTextDatumR{
			LanguageDataPlatformLanguageLanguageDatum: related,
		}
	} else {
		o.R.LanguageDataPlatformLanguageLanguageDatum = related
	}

	return nil
}

// DataPlatformCountryCountryTextData retrieves all the records using an executor.
func DataPlatformCountryCountryTextData(mods ...qm.QueryMod) dataPlatformCountryCountryTextDatumQuery {
	mods = append(mods, qm.From("`data_platform_country_country_text_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_country_country_text_data`.*"})
	}

	return dataPlatformCountryCountryTextDatumQuery{q}
}

// FindDataPlatformCountryCountryTextDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformCountryCountryTextDatum(ctx context.Context, exec boil.ContextExecutor, country string, language string, selectCols ...string) (*DataPlatformCountryCountryTextDatum, error) {
	dataPlatformCountryCountryTextDatumObj := &DataPlatformCountryCountryTextDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_country_country_text_data` where `Country`=? AND `Language`=?", sel,
	)

	q := queries.Raw(query, country, language)

	err := q.Bind(ctx, exec, dataPlatformCountryCountryTextDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_country_country_text_data")
	}

	return dataPlatformCountryCountryTextDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformCountryCountryTextDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_country_country_text_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformCountryCountryTextDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformCountryCountryTextDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformCountryCountryTextDatumInsertCache[key]
	dataPlatformCountryCountryTextDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformCountryCountryTextDatumAllColumns,
			dataPlatformCountryCountryTextDatumColumnsWithDefault,
			dataPlatformCountryCountryTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_country_country_text_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_country_country_text_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_country_country_text_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_country_country_text_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.Country,
		o.Language,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_country_country_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformCountryCountryTextDatumInsertCacheMut.Lock()
		dataPlatformCountryCountryTextDatumInsertCache[key] = cache
		dataPlatformCountryCountryTextDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformCountryCountryTextDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformCountryCountryTextDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformCountryCountryTextDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformCountryCountryTextDatumUpdateCache[key]
	dataPlatformCountryCountryTextDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformCountryCountryTextDatumAllColumns,
			dataPlatformCountryCountryTextDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_country_country_text_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_country_country_text_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, append(wl, dataPlatformCountryCountryTextDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_country_country_text_data row")
	}

	if !cached {
		dataPlatformCountryCountryTextDatumUpdateCacheMut.Lock()
		dataPlatformCountryCountryTextDatumUpdateCache[key] = cache
		dataPlatformCountryCountryTextDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformCountryCountryTextDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_country_country_text_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformCountryCountryTextDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformCountryCountryTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_country_country_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformCountryCountryTextDatum slice")
	}

	return nil
}

var mySQLDataPlatformCountryCountryTextDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformCountryCountryTextDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_country_country_text_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformCountryCountryTextDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformCountryCountryTextDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformCountryCountryTextDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformCountryCountryTextDatumUpsertCache[key]
	dataPlatformCountryCountryTextDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformCountryCountryTextDatumAllColumns,
			dataPlatformCountryCountryTextDatumColumnsWithDefault,
			dataPlatformCountryCountryTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformCountryCountryTextDatumAllColumns,
			dataPlatformCountryCountryTextDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_country_country_text_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_country_country_text_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_country_country_text_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_country_country_text_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformCountryCountryTextDatumType, dataPlatformCountryCountryTextDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_country_country_text_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_country_country_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformCountryCountryTextDatumUpsertCacheMut.Lock()
		dataPlatformCountryCountryTextDatumUpsertCache[key] = cache
		dataPlatformCountryCountryTextDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformCountryCountryTextDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformCountryCountryTextDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformCountryCountryTextDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformCountryCountryTextDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_country_country_text_data` WHERE `Country`=? AND `Language`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_country_country_text_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformCountryCountryTextDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformCountryCountryTextDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_country_country_text_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformCountryCountryTextDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformCountryCountryTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_country_country_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformCountryCountryTextDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformCountryCountryTextDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformCountryCountryTextDatum(ctx, exec, o.Country, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformCountryCountryTextDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformCountryCountryTextDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformCountryCountryTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_country_country_text_data`.* FROM `data_platform_country_country_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformCountryCountryTextDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformCountryCountryTextDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformCountryCountryTextDatumExists checks if the DataPlatformCountryCountryTextDatum row exists.
func DataPlatformCountryCountryTextDatumExists(ctx context.Context, exec boil.ContextExecutor, country string, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_country_country_text_data` where `Country`=? AND `Language`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, country, language)
	}
	row := exec.QueryRowContext(ctx, sql, country, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_country_country_text_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformCountryCountryTextDatum row exists.
func (o *DataPlatformCountryCountryTextDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformCountryCountryTextDatumExists(ctx, exec, o.Country, o.Language)
}
