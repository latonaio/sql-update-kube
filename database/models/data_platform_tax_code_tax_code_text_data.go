// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformTaxCodeTaxCodeTextDatum is an object representing the database table.
type DataPlatformTaxCodeTaxCodeTextDatum struct {
	Country     string      `boil:"Country" json:"Country" toml:"Country" yaml:"Country"`
	TaxCode     string      `boil:"TaxCode" json:"TaxCode" toml:"TaxCode" yaml:"TaxCode"`
	Language    string      `boil:"Language" json:"Language" toml:"Language" yaml:"Language"`
	TaxCodeName null.String `boil:"TaxCodeName" json:"TaxCodeName,omitempty" toml:"TaxCodeName" yaml:"TaxCodeName,omitempty"`

	R *dataPlatformTaxCodeTaxCodeTextDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformTaxCodeTaxCodeTextDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformTaxCodeTaxCodeTextDatumColumns = struct {
	Country     string
	TaxCode     string
	Language    string
	TaxCodeName string
}{
	Country:     "Country",
	TaxCode:     "TaxCode",
	Language:    "Language",
	TaxCodeName: "TaxCodeName",
}

var DataPlatformTaxCodeTaxCodeTextDatumTableColumns = struct {
	Country     string
	TaxCode     string
	Language    string
	TaxCodeName string
}{
	Country:     "data_platform_tax_code_tax_code_text_data.Country",
	TaxCode:     "data_platform_tax_code_tax_code_text_data.TaxCode",
	Language:    "data_platform_tax_code_tax_code_text_data.Language",
	TaxCodeName: "data_platform_tax_code_tax_code_text_data.TaxCodeName",
}

// Generated where

var DataPlatformTaxCodeTaxCodeTextDatumWhere = struct {
	Country     whereHelperstring
	TaxCode     whereHelperstring
	Language    whereHelperstring
	TaxCodeName whereHelpernull_String
}{
	Country:     whereHelperstring{field: "`data_platform_tax_code_tax_code_text_data`.`Country`"},
	TaxCode:     whereHelperstring{field: "`data_platform_tax_code_tax_code_text_data`.`TaxCode`"},
	Language:    whereHelperstring{field: "`data_platform_tax_code_tax_code_text_data`.`Language`"},
	TaxCodeName: whereHelpernull_String{field: "`data_platform_tax_code_tax_code_text_data`.`TaxCodeName`"},
}

// DataPlatformTaxCodeTaxCodeTextDatumRels is where relationship names are stored.
var DataPlatformTaxCodeTaxCodeTextDatumRels = struct {
	CountryDataPlatformTaxCodeTaxCodeDatum string
}{
	CountryDataPlatformTaxCodeTaxCodeDatum: "CountryDataPlatformTaxCodeTaxCodeDatum",
}

// dataPlatformTaxCodeTaxCodeTextDatumR is where relationships are stored.
type dataPlatformTaxCodeTaxCodeTextDatumR struct {
	CountryDataPlatformTaxCodeTaxCodeDatum *DataPlatformTaxCodeTaxCodeDatum `boil:"CountryDataPlatformTaxCodeTaxCodeDatum" json:"CountryDataPlatformTaxCodeTaxCodeDatum" toml:"CountryDataPlatformTaxCodeTaxCodeDatum" yaml:"CountryDataPlatformTaxCodeTaxCodeDatum"`
}

// NewStruct creates a new relationship struct
func (*dataPlatformTaxCodeTaxCodeTextDatumR) NewStruct() *dataPlatformTaxCodeTaxCodeTextDatumR {
	return &dataPlatformTaxCodeTaxCodeTextDatumR{}
}

func (r *dataPlatformTaxCodeTaxCodeTextDatumR) GetCountryDataPlatformTaxCodeTaxCodeDatum() *DataPlatformTaxCodeTaxCodeDatum {
	if r == nil {
		return nil
	}
	return r.CountryDataPlatformTaxCodeTaxCodeDatum
}

// dataPlatformTaxCodeTaxCodeTextDatumL is where Load methods for each relationship are stored.
type dataPlatformTaxCodeTaxCodeTextDatumL struct{}

var (
	dataPlatformTaxCodeTaxCodeTextDatumAllColumns            = []string{"Country", "TaxCode", "Language", "TaxCodeName"}
	dataPlatformTaxCodeTaxCodeTextDatumColumnsWithoutDefault = []string{"Country", "TaxCode", "Language", "TaxCodeName"}
	dataPlatformTaxCodeTaxCodeTextDatumColumnsWithDefault    = []string{}
	dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns     = []string{"Country", "TaxCode", "Language"}
	dataPlatformTaxCodeTaxCodeTextDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformTaxCodeTaxCodeTextDatumSlice is an alias for a slice of pointers to DataPlatformTaxCodeTaxCodeTextDatum.
	// This should almost always be used instead of []DataPlatformTaxCodeTaxCodeTextDatum.
	DataPlatformTaxCodeTaxCodeTextDatumSlice []*DataPlatformTaxCodeTaxCodeTextDatum

	dataPlatformTaxCodeTaxCodeTextDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformTaxCodeTaxCodeTextDatumType                 = reflect.TypeOf(&DataPlatformTaxCodeTaxCodeTextDatum{})
	dataPlatformTaxCodeTaxCodeTextDatumMapping              = queries.MakeStructMapping(dataPlatformTaxCodeTaxCodeTextDatumType)
	dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns)
	dataPlatformTaxCodeTaxCodeTextDatumInsertCacheMut       sync.RWMutex
	dataPlatformTaxCodeTaxCodeTextDatumInsertCache          = make(map[string]insertCache)
	dataPlatformTaxCodeTaxCodeTextDatumUpdateCacheMut       sync.RWMutex
	dataPlatformTaxCodeTaxCodeTextDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformTaxCodeTaxCodeTextDatumUpsertCacheMut       sync.RWMutex
	dataPlatformTaxCodeTaxCodeTextDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformTaxCodeTaxCodeTextDatum record from the query.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformTaxCodeTaxCodeTextDatum, error) {
	o := &DataPlatformTaxCodeTaxCodeTextDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_tax_code_tax_code_text_data")
	}

	return o, nil
}

// All returns all DataPlatformTaxCodeTaxCodeTextDatum records from the query.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformTaxCodeTaxCodeTextDatumSlice, error) {
	var o []*DataPlatformTaxCodeTaxCodeTextDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformTaxCodeTaxCodeTextDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformTaxCodeTaxCodeTextDatum records in the query.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_tax_code_tax_code_text_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_tax_code_tax_code_text_data exists")
	}

	return count > 0, nil
}

// CountryDataPlatformTaxCodeTaxCodeDatum pointed to by the foreign key.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) CountryDataPlatformTaxCodeTaxCodeDatum(mods ...qm.QueryMod) dataPlatformTaxCodeTaxCodeDatumQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`Country` = ?", o.Country),
	}

	queryMods = append(queryMods, mods...)

	return DataPlatformTaxCodeTaxCodeData(queryMods...)
}

// LoadCountryDataPlatformTaxCodeTaxCodeDatum allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dataPlatformTaxCodeTaxCodeTextDatumL) LoadCountryDataPlatformTaxCodeTaxCodeDatum(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDataPlatformTaxCodeTaxCodeTextDatum interface{}, mods queries.Applicator) error {
	var slice []*DataPlatformTaxCodeTaxCodeTextDatum
	var object *DataPlatformTaxCodeTaxCodeTextDatum

	if singular {
		var ok bool
		object, ok = maybeDataPlatformTaxCodeTaxCodeTextDatum.(*DataPlatformTaxCodeTaxCodeTextDatum)
		if !ok {
			object = new(DataPlatformTaxCodeTaxCodeTextDatum)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDataPlatformTaxCodeTaxCodeTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDataPlatformTaxCodeTaxCodeTextDatum))
			}
		}
	} else {
		s, ok := maybeDataPlatformTaxCodeTaxCodeTextDatum.(*[]*DataPlatformTaxCodeTaxCodeTextDatum)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDataPlatformTaxCodeTaxCodeTextDatum)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDataPlatformTaxCodeTaxCodeTextDatum))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &dataPlatformTaxCodeTaxCodeTextDatumR{}
		}
		args = append(args, object.Country)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dataPlatformTaxCodeTaxCodeTextDatumR{}
			}

			for _, a := range args {
				if a == obj.Country {
					continue Outer
				}
			}

			args = append(args, obj.Country)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`data_platform_tax_code_tax_code_data`),
		qm.WhereIn(`data_platform_tax_code_tax_code_data.Country in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DataPlatformTaxCodeTaxCodeDatum")
	}

	var resultSlice []*DataPlatformTaxCodeTaxCodeDatum
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DataPlatformTaxCodeTaxCodeDatum")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for data_platform_tax_code_tax_code_data")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for data_platform_tax_code_tax_code_data")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CountryDataPlatformTaxCodeTaxCodeDatum = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.Country == foreign.Country {
				local.R.CountryDataPlatformTaxCodeTaxCodeDatum = foreign
				break
			}
		}
	}

	return nil
}

// SetCountryDataPlatformTaxCodeTaxCodeDatum of the dataPlatformTaxCodeTaxCodeTextDatum to the related item.
// Sets o.R.CountryDataPlatformTaxCodeTaxCodeDatum to related.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) SetCountryDataPlatformTaxCodeTaxCodeDatum(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DataPlatformTaxCodeTaxCodeDatum) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `data_platform_tax_code_tax_code_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"Country"}),
		strmangle.WhereClause("`", "`", 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns),
	)
	values := []interface{}{related.Country, o.Country, o.TaxCode, o.Language}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.Country = related.Country
	if o.R == nil {
		o.R = &dataPlatformTaxCodeTaxCodeTextDatumR{
			CountryDataPlatformTaxCodeTaxCodeDatum: related,
		}
	} else {
		o.R.CountryDataPlatformTaxCodeTaxCodeDatum = related
	}

	return nil
}

// DataPlatformTaxCodeTaxCodeTextData retrieves all the records using an executor.
func DataPlatformTaxCodeTaxCodeTextData(mods ...qm.QueryMod) dataPlatformTaxCodeTaxCodeTextDatumQuery {
	mods = append(mods, qm.From("`data_platform_tax_code_tax_code_text_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_tax_code_tax_code_text_data`.*"})
	}

	return dataPlatformTaxCodeTaxCodeTextDatumQuery{q}
}

// FindDataPlatformTaxCodeTaxCodeTextDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformTaxCodeTaxCodeTextDatum(ctx context.Context, exec boil.ContextExecutor, country string, taxCode string, language string, selectCols ...string) (*DataPlatformTaxCodeTaxCodeTextDatum, error) {
	dataPlatformTaxCodeTaxCodeTextDatumObj := &DataPlatformTaxCodeTaxCodeTextDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_tax_code_tax_code_text_data` where `Country`=? AND `TaxCode`=? AND `Language`=?", sel,
	)

	q := queries.Raw(query, country, taxCode, language)

	err := q.Bind(ctx, exec, dataPlatformTaxCodeTaxCodeTextDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_tax_code_tax_code_text_data")
	}

	return dataPlatformTaxCodeTaxCodeTextDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_tax_code_tax_code_text_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformTaxCodeTaxCodeTextDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformTaxCodeTaxCodeTextDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformTaxCodeTaxCodeTextDatumInsertCache[key]
	dataPlatformTaxCodeTaxCodeTextDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformTaxCodeTaxCodeTextDatumAllColumns,
			dataPlatformTaxCodeTaxCodeTextDatumColumnsWithDefault,
			dataPlatformTaxCodeTaxCodeTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_tax_code_tax_code_text_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_tax_code_tax_code_text_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_tax_code_tax_code_text_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_tax_code_tax_code_text_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.Country,
		o.TaxCode,
		o.Language,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_tax_code_tax_code_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformTaxCodeTaxCodeTextDatumInsertCacheMut.Lock()
		dataPlatformTaxCodeTaxCodeTextDatumInsertCache[key] = cache
		dataPlatformTaxCodeTaxCodeTextDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformTaxCodeTaxCodeTextDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformTaxCodeTaxCodeTextDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformTaxCodeTaxCodeTextDatumUpdateCache[key]
	dataPlatformTaxCodeTaxCodeTextDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformTaxCodeTaxCodeTextDatumAllColumns,
			dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_tax_code_tax_code_text_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_tax_code_tax_code_text_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, append(wl, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_tax_code_tax_code_text_data row")
	}

	if !cached {
		dataPlatformTaxCodeTaxCodeTextDatumUpdateCacheMut.Lock()
		dataPlatformTaxCodeTaxCodeTextDatumUpdateCache[key] = cache
		dataPlatformTaxCodeTaxCodeTextDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_tax_code_tax_code_text_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformTaxCodeTaxCodeTextDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_tax_code_tax_code_text_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformTaxCodeTaxCodeTextDatum slice")
	}

	return nil
}

var mySQLDataPlatformTaxCodeTaxCodeTextDatumUniqueColumns = []string{}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_tax_code_tax_code_text_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformTaxCodeTaxCodeTextDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformTaxCodeTaxCodeTextDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformTaxCodeTaxCodeTextDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformTaxCodeTaxCodeTextDatumUpsertCache[key]
	dataPlatformTaxCodeTaxCodeTextDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformTaxCodeTaxCodeTextDatumAllColumns,
			dataPlatformTaxCodeTaxCodeTextDatumColumnsWithDefault,
			dataPlatformTaxCodeTaxCodeTextDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformTaxCodeTaxCodeTextDatumAllColumns,
			dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_tax_code_tax_code_text_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_tax_code_tax_code_text_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_tax_code_tax_code_text_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_tax_code_tax_code_text_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformTaxCodeTaxCodeTextDatumType, dataPlatformTaxCodeTaxCodeTextDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_tax_code_tax_code_text_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_tax_code_tax_code_text_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformTaxCodeTaxCodeTextDatumUpsertCacheMut.Lock()
		dataPlatformTaxCodeTaxCodeTextDatumUpsertCache[key] = cache
		dataPlatformTaxCodeTaxCodeTextDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformTaxCodeTaxCodeTextDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformTaxCodeTaxCodeTextDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_tax_code_tax_code_text_data` WHERE `Country`=? AND `TaxCode`=? AND `Language`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_tax_code_tax_code_text_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformTaxCodeTaxCodeTextDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformTaxCodeTaxCodeTextDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_tax_code_tax_code_text_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformTaxCodeTaxCodeTextDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_tax_code_tax_code_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformTaxCodeTaxCodeTextDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformTaxCodeTaxCodeTextDatum(ctx, exec, o.Country, o.TaxCode, o.Language)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformTaxCodeTaxCodeTextDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformTaxCodeTaxCodeTextDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_tax_code_tax_code_text_data`.* FROM `data_platform_tax_code_tax_code_text_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformTaxCodeTaxCodeTextDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformTaxCodeTaxCodeTextDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformTaxCodeTaxCodeTextDatumExists checks if the DataPlatformTaxCodeTaxCodeTextDatum row exists.
func DataPlatformTaxCodeTaxCodeTextDatumExists(ctx context.Context, exec boil.ContextExecutor, country string, taxCode string, language string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_tax_code_tax_code_text_data` where `Country`=? AND `TaxCode`=? AND `Language`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, country, taxCode, language)
	}
	row := exec.QueryRowContext(ctx, sql, country, taxCode, language)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_tax_code_tax_code_text_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformTaxCodeTaxCodeTextDatum row exists.
func (o *DataPlatformTaxCodeTaxCodeTextDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformTaxCodeTaxCodeTextDatumExists(ctx, exec, o.Country, o.TaxCode, o.Language)
}
