// Code generated by SQLBoiler 4.13.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DataPlatformBusinessPartnerAccountingDatum is an object representing the database table.
type DataPlatformBusinessPartnerAccountingDatum struct {
	BusinessPartner     int         `boil:"BusinessPartner" json:"BusinessPartner" toml:"BusinessPartner" yaml:"BusinessPartner"`
	ChartOfAccounts     null.String `boil:"ChartOfAccounts" json:"ChartOfAccounts,omitempty" toml:"ChartOfAccounts" yaml:"ChartOfAccounts,omitempty"`
	FiscalYearVariant   null.String `boil:"FiscalYearVariant" json:"FiscalYearVariant,omitempty" toml:"FiscalYearVariant" yaml:"FiscalYearVariant,omitempty"`
	IsMarkedForDeletion null.Bool   `boil:"IsMarkedForDeletion" json:"IsMarkedForDeletion,omitempty" toml:"IsMarkedForDeletion" yaml:"IsMarkedForDeletion,omitempty"`

	R *dataPlatformBusinessPartnerAccountingDatumR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dataPlatformBusinessPartnerAccountingDatumL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DataPlatformBusinessPartnerAccountingDatumColumns = struct {
	BusinessPartner     string
	ChartOfAccounts     string
	FiscalYearVariant   string
	IsMarkedForDeletion string
}{
	BusinessPartner:     "BusinessPartner",
	ChartOfAccounts:     "ChartOfAccounts",
	FiscalYearVariant:   "FiscalYearVariant",
	IsMarkedForDeletion: "IsMarkedForDeletion",
}

var DataPlatformBusinessPartnerAccountingDatumTableColumns = struct {
	BusinessPartner     string
	ChartOfAccounts     string
	FiscalYearVariant   string
	IsMarkedForDeletion string
}{
	BusinessPartner:     "data_platform_business_partner_accounting_data.BusinessPartner",
	ChartOfAccounts:     "data_platform_business_partner_accounting_data.ChartOfAccounts",
	FiscalYearVariant:   "data_platform_business_partner_accounting_data.FiscalYearVariant",
	IsMarkedForDeletion: "data_platform_business_partner_accounting_data.IsMarkedForDeletion",
}

// Generated where

var DataPlatformBusinessPartnerAccountingDatumWhere = struct {
	BusinessPartner     whereHelperint
	ChartOfAccounts     whereHelpernull_String
	FiscalYearVariant   whereHelpernull_String
	IsMarkedForDeletion whereHelpernull_Bool
}{
	BusinessPartner:     whereHelperint{field: "`data_platform_business_partner_accounting_data`.`BusinessPartner`"},
	ChartOfAccounts:     whereHelpernull_String{field: "`data_platform_business_partner_accounting_data`.`ChartOfAccounts`"},
	FiscalYearVariant:   whereHelpernull_String{field: "`data_platform_business_partner_accounting_data`.`FiscalYearVariant`"},
	IsMarkedForDeletion: whereHelpernull_Bool{field: "`data_platform_business_partner_accounting_data`.`IsMarkedForDeletion`"},
}

// DataPlatformBusinessPartnerAccountingDatumRels is where relationship names are stored.
var DataPlatformBusinessPartnerAccountingDatumRels = struct {
}{}

// dataPlatformBusinessPartnerAccountingDatumR is where relationships are stored.
type dataPlatformBusinessPartnerAccountingDatumR struct {
}

// NewStruct creates a new relationship struct
func (*dataPlatformBusinessPartnerAccountingDatumR) NewStruct() *dataPlatformBusinessPartnerAccountingDatumR {
	return &dataPlatformBusinessPartnerAccountingDatumR{}
}

// dataPlatformBusinessPartnerAccountingDatumL is where Load methods for each relationship are stored.
type dataPlatformBusinessPartnerAccountingDatumL struct{}

var (
	dataPlatformBusinessPartnerAccountingDatumAllColumns            = []string{"BusinessPartner", "ChartOfAccounts", "FiscalYearVariant", "IsMarkedForDeletion"}
	dataPlatformBusinessPartnerAccountingDatumColumnsWithoutDefault = []string{"BusinessPartner", "ChartOfAccounts", "FiscalYearVariant", "IsMarkedForDeletion"}
	dataPlatformBusinessPartnerAccountingDatumColumnsWithDefault    = []string{}
	dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns     = []string{"BusinessPartner"}
	dataPlatformBusinessPartnerAccountingDatumGeneratedColumns      = []string{}
)

type (
	// DataPlatformBusinessPartnerAccountingDatumSlice is an alias for a slice of pointers to DataPlatformBusinessPartnerAccountingDatum.
	// This should almost always be used instead of []DataPlatformBusinessPartnerAccountingDatum.
	DataPlatformBusinessPartnerAccountingDatumSlice []*DataPlatformBusinessPartnerAccountingDatum

	dataPlatformBusinessPartnerAccountingDatumQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dataPlatformBusinessPartnerAccountingDatumType                 = reflect.TypeOf(&DataPlatformBusinessPartnerAccountingDatum{})
	dataPlatformBusinessPartnerAccountingDatumMapping              = queries.MakeStructMapping(dataPlatformBusinessPartnerAccountingDatumType)
	dataPlatformBusinessPartnerAccountingDatumPrimaryKeyMapping, _ = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns)
	dataPlatformBusinessPartnerAccountingDatumInsertCacheMut       sync.RWMutex
	dataPlatformBusinessPartnerAccountingDatumInsertCache          = make(map[string]insertCache)
	dataPlatformBusinessPartnerAccountingDatumUpdateCacheMut       sync.RWMutex
	dataPlatformBusinessPartnerAccountingDatumUpdateCache          = make(map[string]updateCache)
	dataPlatformBusinessPartnerAccountingDatumUpsertCacheMut       sync.RWMutex
	dataPlatformBusinessPartnerAccountingDatumUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dataPlatformBusinessPartnerAccountingDatum record from the query.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DataPlatformBusinessPartnerAccountingDatum, error) {
	o := &DataPlatformBusinessPartnerAccountingDatum{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for data_platform_business_partner_accounting_data")
	}

	return o, nil
}

// All returns all DataPlatformBusinessPartnerAccountingDatum records from the query.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) All(ctx context.Context, exec boil.ContextExecutor) (DataPlatformBusinessPartnerAccountingDatumSlice, error) {
	var o []*DataPlatformBusinessPartnerAccountingDatum

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DataPlatformBusinessPartnerAccountingDatum slice")
	}

	return o, nil
}

// Count returns the count of all DataPlatformBusinessPartnerAccountingDatum records in the query.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count data_platform_business_partner_accounting_data rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if data_platform_business_partner_accounting_data exists")
	}

	return count > 0, nil
}

// DataPlatformBusinessPartnerAccountingData retrieves all the records using an executor.
func DataPlatformBusinessPartnerAccountingData(mods ...qm.QueryMod) dataPlatformBusinessPartnerAccountingDatumQuery {
	mods = append(mods, qm.From("`data_platform_business_partner_accounting_data`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`data_platform_business_partner_accounting_data`.*"})
	}

	return dataPlatformBusinessPartnerAccountingDatumQuery{q}
}

// FindDataPlatformBusinessPartnerAccountingDatum retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDataPlatformBusinessPartnerAccountingDatum(ctx context.Context, exec boil.ContextExecutor, businessPartner int, selectCols ...string) (*DataPlatformBusinessPartnerAccountingDatum, error) {
	dataPlatformBusinessPartnerAccountingDatumObj := &DataPlatformBusinessPartnerAccountingDatum{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `data_platform_business_partner_accounting_data` where `BusinessPartner`=?", sel,
	)

	q := queries.Raw(query, businessPartner)

	err := q.Bind(ctx, exec, dataPlatformBusinessPartnerAccountingDatumObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from data_platform_business_partner_accounting_data")
	}

	return dataPlatformBusinessPartnerAccountingDatumObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DataPlatformBusinessPartnerAccountingDatum) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_business_partner_accounting_data provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformBusinessPartnerAccountingDatumColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dataPlatformBusinessPartnerAccountingDatumInsertCacheMut.RLock()
	cache, cached := dataPlatformBusinessPartnerAccountingDatumInsertCache[key]
	dataPlatformBusinessPartnerAccountingDatumInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dataPlatformBusinessPartnerAccountingDatumAllColumns,
			dataPlatformBusinessPartnerAccountingDatumColumnsWithDefault,
			dataPlatformBusinessPartnerAccountingDatumColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `data_platform_business_partner_accounting_data` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `data_platform_business_partner_accounting_data` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `data_platform_business_partner_accounting_data` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into data_platform_business_partner_accounting_data")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.BusinessPartner,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_business_partner_accounting_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformBusinessPartnerAccountingDatumInsertCacheMut.Lock()
		dataPlatformBusinessPartnerAccountingDatumInsertCache[key] = cache
		dataPlatformBusinessPartnerAccountingDatumInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DataPlatformBusinessPartnerAccountingDatum.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DataPlatformBusinessPartnerAccountingDatum) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	var err error
	key := makeCacheKey(columns, nil)
	dataPlatformBusinessPartnerAccountingDatumUpdateCacheMut.RLock()
	cache, cached := dataPlatformBusinessPartnerAccountingDatumUpdateCache[key]
	dataPlatformBusinessPartnerAccountingDatumUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dataPlatformBusinessPartnerAccountingDatumAllColumns,
			dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update data_platform_business_partner_accounting_data, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `data_platform_business_partner_accounting_data` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, append(wl, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	_, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update data_platform_business_partner_accounting_data row")
	}

	if !cached {
		dataPlatformBusinessPartnerAccountingDatumUpdateCacheMut.Lock()
		dataPlatformBusinessPartnerAccountingDatumUpdateCache[key] = cache
		dataPlatformBusinessPartnerAccountingDatumUpdateCacheMut.Unlock()
	}

	return nil
}

// UpdateAll updates all rows with the specified column values.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for data_platform_business_partner_accounting_data")
	}

	return nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DataPlatformBusinessPartnerAccountingDatumSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformBusinessPartnerAccountingDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `data_platform_business_partner_accounting_data` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in dataPlatformBusinessPartnerAccountingDatum slice")
	}

	return nil
}

var mySQLDataPlatformBusinessPartnerAccountingDatumUniqueColumns = []string{
	"BusinessPartner",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DataPlatformBusinessPartnerAccountingDatum) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no data_platform_business_partner_accounting_data provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dataPlatformBusinessPartnerAccountingDatumColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLDataPlatformBusinessPartnerAccountingDatumUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dataPlatformBusinessPartnerAccountingDatumUpsertCacheMut.RLock()
	cache, cached := dataPlatformBusinessPartnerAccountingDatumUpsertCache[key]
	dataPlatformBusinessPartnerAccountingDatumUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dataPlatformBusinessPartnerAccountingDatumAllColumns,
			dataPlatformBusinessPartnerAccountingDatumColumnsWithDefault,
			dataPlatformBusinessPartnerAccountingDatumColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dataPlatformBusinessPartnerAccountingDatumAllColumns,
			dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert data_platform_business_partner_accounting_data, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`data_platform_business_partner_accounting_data`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `data_platform_business_partner_accounting_data` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for data_platform_business_partner_accounting_data")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(dataPlatformBusinessPartnerAccountingDatumType, dataPlatformBusinessPartnerAccountingDatumMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for data_platform_business_partner_accounting_data")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for data_platform_business_partner_accounting_data")
	}

CacheNoHooks:
	if !cached {
		dataPlatformBusinessPartnerAccountingDatumUpsertCacheMut.Lock()
		dataPlatformBusinessPartnerAccountingDatumUpsertCache[key] = cache
		dataPlatformBusinessPartnerAccountingDatumUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DataPlatformBusinessPartnerAccountingDatum record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DataPlatformBusinessPartnerAccountingDatum) Delete(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil {
		return errors.New("models: no DataPlatformBusinessPartnerAccountingDatum provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dataPlatformBusinessPartnerAccountingDatumPrimaryKeyMapping)
	sql := "DELETE FROM `data_platform_business_partner_accounting_data` WHERE `BusinessPartner`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from data_platform_business_partner_accounting_data")
	}

	return nil
}

// DeleteAll deletes all matching rows.
func (q dataPlatformBusinessPartnerAccountingDatumQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if q.Query == nil {
		return errors.New("models: no dataPlatformBusinessPartnerAccountingDatumQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from data_platform_business_partner_accounting_data")
	}

	return nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DataPlatformBusinessPartnerAccountingDatumSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) error {
	if len(o) == 0 {
		return nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformBusinessPartnerAccountingDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `data_platform_business_partner_accounting_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	_, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from dataPlatformBusinessPartnerAccountingDatum slice")
	}

	return nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DataPlatformBusinessPartnerAccountingDatum) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDataPlatformBusinessPartnerAccountingDatum(ctx, exec, o.BusinessPartner)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DataPlatformBusinessPartnerAccountingDatumSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DataPlatformBusinessPartnerAccountingDatumSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dataPlatformBusinessPartnerAccountingDatumPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `data_platform_business_partner_accounting_data`.* FROM `data_platform_business_partner_accounting_data` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, dataPlatformBusinessPartnerAccountingDatumPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DataPlatformBusinessPartnerAccountingDatumSlice")
	}

	*o = slice

	return nil
}

// DataPlatformBusinessPartnerAccountingDatumExists checks if the DataPlatformBusinessPartnerAccountingDatum row exists.
func DataPlatformBusinessPartnerAccountingDatumExists(ctx context.Context, exec boil.ContextExecutor, businessPartner int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `data_platform_business_partner_accounting_data` where `BusinessPartner`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, businessPartner)
	}
	row := exec.QueryRowContext(ctx, sql, businessPartner)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if data_platform_business_partner_accounting_data exists")
	}

	return exists, nil
}

// Exists checks if the DataPlatformBusinessPartnerAccountingDatum row exists.
func (o *DataPlatformBusinessPartnerAccountingDatum) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DataPlatformBusinessPartnerAccountingDatumExists(ctx, exec, o.BusinessPartner)
}
